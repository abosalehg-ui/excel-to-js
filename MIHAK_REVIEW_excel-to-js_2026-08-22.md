# مِحَك — مراجعة شاملة: excel-to-js (v3.1.3)

**تاريخ المراجعة:** 2026-08-22 · **النوع المكتشف:** تطبيق (أداة متصفح) — لا حلقة لعب ولا محرك رسم، فطُبّق رابرك التطبيقات
**الأوزان:** هندسة 45% · UX/UI 30% · أمن 25%
**النطاق:** المستودع كامل — 22 ملفًا، ~2,750 سطر كود تطبيق + ~1,625 سطر اختبارات. قُرئت كل ملفات المصدر بالكامل.
**التحقق:** شُغّلت `npm ci` و`node tests/run-node.js` (‏196/196 نجحت)، وكل خطأ في هذا التقرير أُعيد إنتاجه فعليًا بتنفيذ الكود المولّد.

> **الدرجة الإجمالية: 7.9 / 10**
>
> هذي الجولة الثالثة على المشروع (بعد `REVIEW-2.md` بـ7.1 و`CODE_REVIEW_…_2026-08-04.md` بـ7.5)، وخطة الـ22 بندًا اللي نُفّذت في v3.1.3 رفعت المستوى فعليًا: التلوين التركيبي اشتغل، والتباينات صارت مطابقة لـWCAG AA، و`innerHTML` انتُزع من الملف كله، و`ctx.once` قتل النمو الأسّي. الملاحظات اللي بقيت مو تكرارًا للقديم — كلها جديدة، ومعظمها في مكان واحد: **دلالات Excel في الحالات الحدّية**.
>
> **أخطر ملاحظة واحدة:** `=A1+A2` على خليتين نصّيتين رقميتين (`"5"` و`"3"`) يعطي `"53"` بدل `8` — بلا خطأ ولا تحذير. باقي العوامل (`-` `*` `/`) تحوّل صح، فالمشكلة في `+` وحده.

---

## 1. نظرة عامة سريعة

`excel-to-js` أداة متصفح محلية بالكامل تاخذ صيغة Excel نصّية وتخرّج منها دالة JavaScript جاهزة اسمها `calculate(...)`، مع helpers مولّدة عند الحاجة فقط. تشتغل بفتح `index.html` مباشرة — بلا خادم، بلا شبكة، بلا أي تبعية وقت تشغيل.

**البنية:** أربع طبقات مفصولة بوضوح تُحمَّل بالترتيب:

```
helpers.js   → قاموس HELPERS (17 helper، كل واحد { code, deps? })
functions.js → قاموس FUNCTIONS (44 دالة، كل وحدة عقد كامل: minArgs/maxArgs/matrixArgs/defaults/usesHelpers)
engine.js    → Tokenizer (regex بعلم y) → Parser (recursive descent) → Generator (+ ترتيب topological للـhelpers)
ui.js        → كل ما يلمس الـDOM، محصور داخل initUI(root) فيبقى قابلًا للاختبار خارج المتصفح
```

**التقنيات:** JavaScript عادي (ES2019+) بنمط IIFE/UMD يشتغل في المتصفح وNode معًا. بلا إطار عمل، بلا أداة بناء. تبعيات التطوير فقط: `jsdom ^25.0.1` و`prettier ^3.3.3`. الـCI على GitHub Actions (‏`actions/checkout` و`setup-node` مثبّتان على SHA) يشغّل فحص التنسيق + مجموعة الاختبارات مرتين: مرة بتوقيت النظام ومرة بـ`TZ=America/New_York` لالتقاط أخطاء التوقيت الصيفي.

**الحجم:**

| الملف | الأسطر |
|---|---:|
| `index.html` (فيه كل الـCSS) | 687 |
| `assets/engine.js` | 659 |
| `assets/ui.js` | 591 |
| `assets/functions.js` | 505 |
| `assets/helpers.js` | 303 |
| `tests/` (4 ملفات) | 1,625 |
| `README.md` | 372 |

---

## 2. المراجعة التقنية والهندسية — **8.0 / 10** (وزن 45%)

### 2.1 البنية المعمارية وفصل المسؤوليات — **9 / 10**

الفصل حقيقي مو اسمي. أقوى قرار معماري في المشروع إن `FUNCTIONS` سجلّ بيانات لا شجرة `switch`: كل دالة تعلن عقدها، والـengine يطبّق العقد مركزيًا. النتيجة إن إضافة دالة جديدة = كائن واحد في `functions.js` بلا لمس `engine.js` إطلاقًا.

`assets/engine.js` — الفحص المركزي لعدد الوسائط:

```js
const min = fn.minArgs ?? 0;
const max = fn.maxArgs ?? Infinity;
if (argc < min || argc > max) { … }
```

وكذلك `matrixArgs` اللي تخلي `VLOOKUP` تستقبل الجدول ثنائي الأبعاد بينما `SUM` تستقبله مسطّحًا — بقرار من تعريف الدالة لا من شرط داخل الـgenerator. و`deps` + الترتيب الـtopological يضمن إن `_substitute` (اللي يعتمد على `_str`) ما يجي قبل تعريف `_str` في المخرجات.

**الخصم الوحيد:** `generate()` في `engine.js` دالة بطول ~200 سطر تحتضن ثمان دوال متداخلة (`cellVar`, `rangeBounds`, `expandRangeToMatrix`, `rangeParamName`, `registerHelper`, `gen`, `topoVisit`…). متماسكة ومبرَّرة (كلها تشترك في حالة `usedCells`/`usedRanges`/`tmpId`)، لكنها أطول من اللازم للقراءة دفعة واحدة.
**الحل:** استخراج `gen` وحدها إلى دالة مستوى-وحدة تاخذ كائن الحالة صراحةً — يقصّر `generate` للنصف بلا تغيير سلوك.

### 2.2 قابلية القراءة والصيانة — **9 / 10**

التعليقات في هذا المشروع من أفضل ما يُقرأ: تشرح **ليش** وتذكر **الخطأ اللي كانت تسببه** بدل ما تكرر الكود. أمثلة حقيقية:

> `engine.js`: «الأنماط بعلم y (sticky) وتُطابَق عند lastIndex مباشرة، فلا يُنشَأ نص جديد لكل token (كان `src.slice(pos)` يجعلها O(n²))»
>
> `engine.js` عن `ctx.once`: «بدونها ينمو الناتج أسّياً عند التداخل (ISBLANK المتداخلة 11 مرة كانت تولّد 3.9 ميغابايت)»
>
> `ui.js` عن `copyCode`: «finally لا بعد الـtry: لو رمت `select()` كانت العقدة تبقى يتيمة في الـDOM»

هذا مستوى توثيق يخلّي مطورًا جديدًا يفهم القرار ولا ينقضه بحسن نية. التسمية متسقة (`$` بادئة لعقد الـDOM، `_` بادئة للـhelpers المولّدة، camelCase في كل مكان).

### 2.3 التكرار (DRY) و Code Smells — **8 / 10**

التكرار اللي رصدته الجولات السابقة انحلّ فعليًا: `_nums` صار helper واحدًا تستعمله `SUM`/`AVERAGE`/`MIN`/`MAX`/`COUNT`، و`_str` وحّد كل `String(x)`، و`exactArg` وحّد منطق الوسيط الرابع بين `VLOOKUP` و`HLOOKUP`، وعدّاد الدوال له مصدر حقيقة واحد (`Object.keys(FUNCTIONS).length`) يغذّي ثلاثة مواضع في الصفحة.

**المتبقّي — قائمة الفئات مكرّرة في ثلاثة مواضع غير مترابطة:**

1. `assets/ui.js` — `CAT_LABELS` (الأسماء العربية)
2. `index.html` — سبعة أزواج متغيّرات `--cat-*-bg` / `--cat-*-fg`
3. `README.md` — سبعة عناوين أقسام `### 🟡 منطقية (5)` مع أعداد مكتوبة يدويًا

إضافة فئة ثامنة تتطلّب تعديل ثلاثة ملفات، والأعداد في README تتقادم صامتة (تصادف الآن أنها صحيحة).
**الحل:** الـCSS لا مفرّ من تكراره، لكن أعداد README يمكن توليدها بسكربت `npm run docs:counts` صغير يقرأ `FUNCTIONS` ويحدّث الجداول — أو على الأقل اختبار يقارن أعداد README بالسجلّ الفعلي، على غرار اختبار `UI: CSP guard` الموجود أصلًا.

**Smell ثانوي:** مصفوفة `EXAMPLES` (18 مثالًا) مدفونة داخل `initUI` في `ui.js` رغم إنها بيانات محضة بلا علاقة بالـDOM. مكانها الطبيعي أعلى الملف بجانب `CAT_LABELS`، أو ملف `examples.js` مستقل.

### 2.4 معالجة الأخطاء والحالات الحدّية — **5 / 10**

هنا يسكن كل الضرر الحقيقي في المشروع. البنية العامة لمعالجة الأخطاء ممتازة (مواضع `start`/`end` تُمرَّر مع كل خطأ فيُعلَّم الموضع بدقة في المحرر، والرسائل عربية ومحددة)، لكن **دلالات Excel في الحالات الحدّية فيها ثلاث فجوات تُنتج إجابات خاطئة بصمت**، وكلها غير موثّقة في جدول «الانحرافات المعروفة» في README.

#### 🔴 (أ) عامل `+` يدمج النصوص بدل ما يجمعها

`assets/engine.js` — في `gen()`، حالة `Binary`:

```js
if (node.op === '&') { registerHelper('_str'); return `(_str(${L}) + _str(${R}))`; }
if (node.op === '^') return `Math.pow(${L}, ${R})`;
if (node.op === '=') { … }
return `(${L} ${opMap[node.op]} ${R})`;   // ← ‏'+' يسقط هنا
```

`+` يُولَّد كـ`+` جافاسكربت خام. وفي JS `+` يعني الدمج النصي إذا كان أحد الطرفين نصًّا، بينما في Excel `+` عملية حسابية دائمًا.

**أُعيد إنتاجه فعليًا:**

| الصيغة | المدخلات | الناتج الحالي | الصحيح في Excel |
|---|---|---|---|
| `=A1+A2` | `a1="5"`, `a2="3"` | **`"53"`** | `8` |
| `=A1-A2` | `a1="5"`, `a2="3"` | `2` ✓ | `2` |
| `=A1*A2` | `a1="5"`, `a2="3"` | `15` ✓ | `15` |
| `=A1+A2` | `a1="نص"`, `a2=5` | **`"نص5"`** | `#VALUE!` |

الخطورة إن الحالة الأولى **مو حالة نادرة**: أي قيمة جاية من `JSON.parse` أو من حقل `<input>` أو من عمود CSV تصل نصًّا. المستخدم يمرّر `"5"` و`"3"` ويستلم `"53"` بلا أي إشارة إن شيئًا انكسر — وهذا أسوأ من رمي خطأ. وكون `-` و`*` و`/` كلها صحيحة يثبت إن هذا سهو لا قرار تصميم.

**الحل:** helper `_add` يطابق دلالة Excel:

```js
_add: {
  code: `function _add(a, b) {
  const x = Number(a), y = Number(b);
  if (isNaN(x) || isNaN(y)) return '#VALUE!';
  return x + y;
}`
}
```

ثم في `engine.js` قبل سطر `opMap`:

```js
if (node.op === '+') { registerHelper('_add'); return `_add(${L}, ${R})`; }
```

(الخلية الفارغة `null` تعطي `Number(null) === 0` وهو بالضبط سلوك Excel للخلية الفارغة في الجمع.)

#### 🔴 (ب) `TRIM` لا تطوي المسافات الداخلية

`assets/functions.js` — `TRIM`:

```js
generator: (args) => `_str(${args[0]}).trim()`
```

`TRIM` في Excel تسوي شيئين: تحذف الأطراف **وتطوي كل تتابع مسافات داخلي إلى مسافة واحدة** — وهذا هو سبب وجودها أصلًا (تنظيف بيانات ملصوقة). `String.prototype.trim` تسوي الأول فقط.

**مُتحقَّق:** `=TRIM(A1)` على `" a   b "` يعطي `"a   b"`، والصحيح `"a b"`.

يضاعف الإرباك إن جدول المرجع في الواجهة يعرض للمستخدم `str.trim()` كـ«المكافئ في JavaScript» — فالوعد صريح والتنفيذ يطابقه، لكن كلاهما لا يطابق Excel.

**الحل:** في `functions.js` غيّر التوليد إلى `` `_str(${args[0]}).replace(/\s+/g, ' ').trim()` ``، وحدّث `jsEquiv` إلى `str.replace(/\s+/g,' ').trim()`.

#### 🟠 (ج) `REPLACE` لا تحرس وسائطها بينما أخواتها تحرس

في v3.1.3 أُصلحت `LEFT`/`RIGHT`/`MID` لترفض الوسيط غير الصالح — وهذا مذكور صراحةً في README:

> «`LEFT`/`RIGHT`/`MID` ترفض الوسيط غير الصالح: `LEFT(x,-1)` كان يعطي `"hell"` وصار `#VALUE!`»

لكن `REPLACE` بقيت خارج الدفعة:

```js
generator: (args, ctx) => {
  const [str, start, num, newStr] = args;
  return ctx.once(`_str(${str})`, (s) =>
    ctx.once(`(${start}) - 1`, (i) => `(${s}.slice(0, ${i}) + _str(${newStr}) + ${s}.slice(${i} + (${num})))`));
}
```

`start` يدخل `slice` بلا فحص، فالسالب يعني «من النهاية» في JS.

**مُتحقَّق على `a1="HELLO"`:**

| الصيغة | الناتج الحالي | الصحيح |
|---|---|---|
| `=REPLACE(A1,-3,2,"X")` | `"HXLO"` | `#VALUE!` |
| `=REPLACE(A1,0,2,"X")` | `"HELLXELLO"` | `#VALUE!` |

**الحل:** لفّ الناتج بنفس نمط `MID` الموجود في الملف:

```js
ctx.once(start, (st) => `(${st} < 1 || (${num}) < 0 ? '#VALUE!' : /* التوليد الحالي */)`)
```

#### 🟠 (د) حدّ عمق التداخل لا يغطي مسار الـUnary — فينكسر المكدّس

`LIMITS.parseDepth = 64` مطبَّق داخل `parsePrimary()` فقط:

```js
function parsePrimary() {
  if (depth >= LIMITS.parseDepth) { … throw … }
  depth++;
  try { return parsePrimaryInner(); } finally { depth--; }
}
```

لكن `parseUnary()` **تستدعي نفسها** لكل معامل أحادي قبل ما تصل `parsePrimary` إطلاقًا:

```js
function parseUnary() {
  if (peek() && … (peek().value === '-' || peek().value === '+')) {
    const op = eat().value;
    const arg = parseUnary();          // ← تكرار بلا عدّاد
    return { type: 'Unary', op, arg };
  }
  return parsePostfix();
}
```

وبما أن `LIMITS.inputLength` يسمح بـ10,000 حرف، يقدر المستخدم يلصق `=` + آلاف الشرطات + `A1` ويتخطّى الحدّ كليًا. **النتيجتان المُتحقَّقتان:**

- عند ~2,000 معامل: يمرّ الـparser، ثم تنكسر شبكة الأمان `new Function` فيرى المستخدم رسالة **مضلِّلة**: «خطأ داخلي: الكود المولّد غير صالح نحوياً (Maximum call stack size exceeded). **الرجاء الإبلاغ عن هذه الصيغة**» — الأداة تطلب من المستخدم يبلّغ عن عطل، وهو أصلًا مجرّد تجاوز حدّ موارد.
- عند ~9,000 معامل: `parse()` نفسها ترمي `RangeError: Maximum call stack size exceeded` خامًا — بلا `start`/`end`، فالواجهة تعرض نصًّا إنجليزيًا بلا تعليم موضع، وهو النمط الوحيد في التطبيق كله اللي يخرق تعاقد الأخطاء العربية المُموضَعة.

**الحل:** انقل حارس العمق إلى `parseUnary` أيضًا (وكذلك حلقة `parsePostfix` للنسبة المئوية)، أو أبسط: عدّاد مستقل للمعاملات الأحادية المتتالية يرمي نفس رسالة «الصيغة معقّدة جداً» المُموضَعة الموجودة أصلًا.

### 2.5 الأداء واستهلاك الموارد — **8 / 10**

الأداء مُعالَج بجدّية ومقيس، مو متروك للصدفة:

- الـtokenizer بعلم `y` يطابق عند `lastIndex` بلا `slice` — يُلغي السلوك التربيعي.
- `ctx.once` يمنع النمو الأسّي في الوسائط المتكررة (كانت 3.9MB لـ`ISBLANK` متداخلة 11 مرة).
- خمسة حدود مُعرَّفة في `LIMITS` مع رسائل عربية واضحة لكل واحد.
- `scheduleHighlight` يجدول التلوين على `requestAnimationFrame` بدل كل ضغطة مفتاح، و`scheduleConvert` يؤجّل التحويل 300ms.

**قياسي:** صيغة بـ60 نطاقًا (120 باراميتر) تُحوَّل في **4ms**؛ 200 تحويل لصيغة نموذجية (`IF`+`AND`+`VLOOKUP`+`SUM`) في أقل من 100ms إجمالًا. لا يوجد أي مسار حرج مشكوك فيه.

**الخصم:** الحد الوحيد اللي فيه ثغرة هو `parseDepth` (البند 2.4-د) — والثغرة تنتهي بانهيار مكدّس، وهو بالضبط ما وُضعت الحدود لمنعه.

### 2.6 الاختبارات والتغطية — **7 / 10**

**196 اختبارًا، كلها تنجح**، موزّعة على 19 مجموعة، ونوعيتها أعلى من المعتاد:

- الـRuntime لا يفحص النصّ المولّد فقط بل **ينفّذه فعلًا** عبر `new Function` ويقارن القيمة — وهذا اللي يجعل الاختبارات ذات قيمة حقيقية.
- الـCI يشغّل المجموعة **مرتين**: بتوقيت النظام وبـ`TZ=America/New_York` — التقاط واعٍ لأخطاء التوقيت الصيفي في `_datedif`.
- `UI: CSP guard` يعيد حساب الـSHA-256 للسكربت المضمّن ويقارنه بالـhash في وسم الـCSP. حارس ذكي: تعديل السكربت بلا تحديث الـhash يكسر الاختبار بدل ما يكسر الصفحة صامتًا عند المستخدم.
- `UI: DOM` (19 اختبارًا) يشغّل الواجهة داخل jsdom.

**الخصم — والدليل عليه هذا التقرير نفسه:** الأخطاء الثلاثة في البند 2.4 كلها في **فروع غير مغطّاة**، والاختبارات الموجودة تمرّ من جنبها:

```js
// tests/suite.js
assertEqual(runFormula('=TRIM(A1)', { a1: '  hello  ' }), 'hello');       // أطراف فقط، بلا مسافات داخلية
assertEqual(runFormula('=REPLACE(A1, 2, 3, "XY")', { a1: 'abcdef' }), 'aXYef');  // وسائط صالحة فقط
```

ولا يوجد أي اختبار لـ`=A1+A2` بمدخلات نصّية، ولا لسلسلة معاملات أحادية طويلة. كذلك **ما فيه أداة قياس تغطية** (لا `c8` ولا `nyc` في `devDependencies`)، فالفجوات ما تظهر إلا بالقراءة اليدوية.

**الحل:** أضف `c8` وشغّله في الـCI بعتبة (`--lines 85`)، وأضف اختبارات القيم الحدّية الثلاثة مع كل إصلاح.

### 2.7 التوثيق — **9 / 10**

`README.md` (372 سطرًا) من النوع النادر اللي يُقرأ فعلًا: مخطط تدفّق للبنية، شرح وضعَي الإخراج، جداول الدوال الأربع والأربعين، خارطة طريق بأرقام إصدارات، جدول `LIMITS`، و — الأهم — **جدول «الانحرافات المعروفة عن Excel»** يعترف بتسعة فروقات صراحةً (`Infinity` بدل `#DIV/0!`، wildcards غير مدعومة، حساسية الحالة في `<`/`>`…). الاعتراف المكتوب بالانحراف أنفع للمستخدم من ادّعاء المطابقة.

**الخصم الوحيد:** الجدول نفسه **ناقص ثلاثة انحرافات** — وهي بالضبط الثلاثة اللي في البند 2.4. ما دام المشروع اختار سياسة «نوثّق ما ننحرف فيه»، فالانحراف غير الموثّق يكسر التعاقد مرتين: مرة بالخطأ ومرة بالصمت عنه.
**الحل:** بعد إصلاح (أ) و(ب) و(ج) ينتفي السبب. أما ما تختار إبقاءه، فمكانه صفّ في ذاك الجدول.

### 2.8 اتفاقيات التسمية ونمط الكود — **9 / 10**

camelCase متسق للدوال والمتغيّرات، SCREAMING_CASE للثوابت (`LIMITS`, `PATTERNS`, `HELPERS`, `FUNCTIONS`, `CAT_LABELS`)، بادئة `_` للـhelpers المولّدة، بادئة `$` لعقد الـDOM المخزّنة. النمط مفروض آليًا: `.prettierrc.json` + `.editorconfig` + خطوة `prettier --check` في الـCI — فما فيه مجال لانجراف يدوي. لا خلط ولا استثناءات رصدتها.

### متوسط الفئة الهندسية

`(9 + 9 + 8 + 5 + 8 + 7 + 9 + 9) ÷ 8 = 64 ÷ 8 = ` **8.0 / 10**

---

## 3. المظهر والتصميم وتجربة المستخدم — **7.3 / 10** (وزن 30%)

### 3.1 الاتساق البصري — **9 / 10**

نظام رموز (design tokens) كامل ومطبَّق فعلًا: `--bg`, `--surface`, `--surface-2`, `--border`, `--text` بثلاث درجات، `--radius`, `--shadow`، ونسخة ليلية كاملة تحت `[data-theme="dark"]`. حتى شارات الفئات السبع صارت متغيّرات (`--cat-logic-bg` …) بعد ما كانت 14 قيمة سداسية خام.

القرار الأدقّ هو فصل `--primary` عن `--primary-solid`، والتعليق يشرح ليش:

> «‏`#4a7fc1` رسب في الدورين معاً: 4.08:1 كمقدمة على السطح، و4.12:1 كخلفية تحت نص أبيض. فأصبح لكل دور قيمته.»

هذا مستوى انضباط لوني نادر في مشروع بهذا الحجم. وفيه اختبار `UI: CSS guard` (9 اختبارات) يحرس القواعد من الانجراف.

### 3.2 سهولة الاستخدام ووضوح التنقّل — **7 / 10**

المهمة الأساسية بصفر نقرات: اكتب/الصق الصيغة → الكود يظهر تلقائيًا بعد 300ms. الشاشة الواحدة تضم كل شيء (محرر · مخرَج · 18 مثالًا بنقرة · جدول مرجعي بـ44 دالة مع بحث نصّي وتصفية بالفئة). و`Ctrl+Enter` مختصر منطقي معلن في الواجهة.

**فجوتان حقيقيتان:**

**(أ) الفشل صامت بينما النجاح مسموع.** في `ui.js` داخل `doConvert`:

```js
if (silent) { hideStatus(); showOutput(''); renderHighlight(input); return; }
```

مبرَّر بتعليق سليم («الصيغة نصف المكتوبة خاطئة دائماً، وإظهار الخطأ لكل حرف ضجيج»)، لكنه يترك حالة شائعة بلا علاج: **مستخدم يلصق صيغة كاملة فيها دالة غير مدعومة** (مثلًا `=SUMIF(...)`، وهي في خارطة الطريق) → لصقه يطلق حدث `input` → تحويل صامت → فشل → يرى لوحة مخرَج فاضية مكتوب فيها «سيظهر الكود هنا بعد التحويل…» وشريط حالة مخفي. **صفر تفسير، وصفر إشارة إلى أن زرّ «تحويل» سيخبره بالسبب.** ولاحظ عدم التناظر: النجاح الصامت **يعرض** شريط نجاح، والفشل الصامت يخفي كل شيء.
**الحل:** فرّق بين اللصق والكتابة — إذا كان طول الإدخال قفز أكثر من ~10 أحرف دفعة واحدة (لصق) اعرض الخطأ كاملًا؛ أو على الأقل عند الفشل الصامت اعرض تلميحًا محايدًا في لوحة المخرَج: «الصيغة غير مكتملة أو غير مدعومة — اضغط تحويل للتفاصيل».

**(ب) لا إرشاد إلى وضع النطاقات عند الحاجة إليه.** `=SUM(A1:A10, B1:B10, …)` بستين نطاقًا يولّد `calculate()` بـ**120 باراميتر** — توقيع غير قابل للاستدعاء عمليًا. الحل موجود في المنتج (مفتاح «النطاقات كمصفوفات») لكن المستخدم لازم يكتشفه بنفسه.
**الحل:** في `showOutputInfo` بـ`ui.js`، إذا كان `paramNames.length > 15` والمفتاح مطفأ، أضف سطرًا: «💡 عدد الباراميترات كبير — جرّب وضع النطاقات كمصفوفات».

### 3.3 الاستجابة عبر أحجام الشاشات — **8 / 10**

نقطتا كسر مدروستان (1024px و640px) وشغل جوال جادّ: زرّ التحويل يصير ثابتًا أسفل الشاشة مع `padding-bottom: 120px` على الحاوية، وكل الأهداف اللمسية `min-height: 44px` (توصية WCAG 2.5.5)، والجدول المرجعي والشريط يمرّران داخليًا. والتعليقات تشرح التفاصيل الخفية:

> «‏`min-width:0` يلغي `min-width:auto` الافتراضي لعنصر flex — بدونه لا ينكمش الشريط ويجرّ الصفحة كلها لتمرير أفقي»

وفيه قسم `@media print` كامل (‏الكود مُخرَج يُشارَك، فالطباعة حالة استعمال حقيقية) — التفاتة أبعد من المتوقع.

**الخصم:** على الجوال يُخفى عمود «الوصف» كليًا:

```css
.ref-table th:nth-child(4), .ref-table td:nth-child(4) { display: none; }
```

الجدول أصلًا قابل للتمرير أفقيًا (`min-width: 560px`)، فالإخفاء يحذف **الشرح العربي** — وهو أنفع الأعمدة للمستخدم الجديد — بلا بديل يوصله إليه.
**الحل:** بدل `display:none`، انقل الوصف تحت اسم الدالة داخل نفس الخلية على الجوال، أو اعرضه بنقرة على الصف.

### 3.4 دعم العربية و RTL — **8 / 10**

المعالجة واعية، مو مجرّد `dir="rtl"` على `<html>`:

- **جزر LTR مقصودة:** المحرر والمخرَج وكل `<code>` تُصرَّح `direction: ltr; text-align: left` — صحيح، لأن صيغة Excel وكود JS نصّان لاتينيان يفسدهما الاتجاه العربي. والتعليق يشرح فخًّا حقيقيًا: «المتصفحات تفرض `text-align: start` على `<textarea>` من الـUA stylesheet فلا يرث من الأب، لذا نصرّح به هنا على الطبقتين معاً» — وهذه بالضبط علّة «النص المزدوج» في الجولة السابقة.
- **خصائص منطقية لا فيزيائية:** `inset-inline-start`, `border-inline-start`, `text-align: start` — فالتخطيط ينعكس صح.
- **`<bdi>` حول رسائل الخطأ** في `ui.js`، لأن الرسالة تدمج شظايا لاتينية (`"A1"`, `RPAREN`) داخل نص عربي فتنقلب مواضع الأقواس بدونه. تفصيلة يغفلها أكثر المطوّرين.

**الخصم:** حزمة الخط `'Segoe UI', 'Noto Sans Arabic', Tahoma, system-ui, …`. أُصلحت في الجولة السابقة بإضافة `Noto Sans Arabic`، لكن الخط **غير محزوم** ولا يمكن تحميله (‏`default-src 'none'` في الـCSP بلا `font-src` يمنع أي خط خارجي — وهذا صحيح لأداة أوفلاين). على لينكس/أندرويد بلا `Noto Sans Arabic` مثبَّتًا محليًا، يسقط التصيير إلى خط النظام العام. الحل الحقيقي الوحيد المتوافق مع مبدأ «أوفلاين بالكامل» هو حزم خط عربي مضغوط كـ`data:` URI داخل `index.html` (‏`img-src 'self' data:` موجود؛ يلزم إضافة `font-src data:`) — وهذا يضخّم الملف بمئات الكيلوبايتات. **توصيتي: لا تفعلها**، بل اذكر الاعتماد على خطوط النظام في README كقرار واعٍ.
لم أفحص شكل الأرقام (هندية مقابل عربية) لأن المشروع لا يصيّر أرقامًا عربية إطلاقًا — كل الأرقام المعروضة جزء من كود لاتيني.

### 3.5 إمكانية الوصول — **6 / 10**

الأساس مبنيّ بجدّية: رابط تخطٍّ، `sr-only` مع `for` صحيح لحقل البحث، `aria-label` على المحرر وزرّ الثيم، `aria-pressed` مُحدَّث ديناميكيًا على أزرار التصفية والثيم، `aria-hidden` على طبقة التلوين وأرقام الأسطر والأيقونات الزخرفية، `:focus-visible` معرَّف عالميًا، `@media (prefers-reduced-motion)`، `<noscript>` عربي واضح، وبطاقات الأمثلة `<button>` لا `<div>` (والتعليق يذكر السبب: «يحلّ التنقل بلوحة المفاتيح والدلالة والتفعيل بـEnter/Space»). والتباينات كلها مُدقَّقة على عتبة 4.5:1 في الوضعين مع توثيق القيم الراسبة.

**ثلاث فجوات مؤكَّدة (بحثت عن الخصائص فما وُجدت في أي ملف):**

| # | الفجوة | الأثر |
|---|---|---|
| 1 | لا `aria-invalid` ولا `aria-describedby` تربط رسالة الخطأ بالـ`<textarea>` | قارئ الشاشة عند التركيز على الحقل لا يعرف إطلاقًا إن فيه خطأ ولا وش هو. التعليم البصري `.err-mark` مرئي بحت. |
| 2 | `.output-wrap` (‏`overflow: auto`) و`.ref-table-wrap` على الجوال بلا `tabindex="0"` | مستخدم لوحة المفاتيح لا يقدر يمرّر لوحة الكود إطلاقًا — خرق **WCAG 2.1.1 (Keyboard)** لأن محتوى موجود لكن غير قابل للوصول. |
| 3 | لا `@media (forced-colors: active)` | التصميم يعتمد `color: transparent` على الـtextarea فوق طبقة تلوين. في وضع التباين العالي بويندوز يُفرَض لون النص، فيظهر **نصّان متراكبان** — نفس عطل الجولة السابقة لكن لفئة مستخدمين أخرى. |

**الحلول:** (1) عند الخطأ في `doConvert` ضع `$input.setAttribute('aria-invalid','true')` و`aria-describedby="status"`، وانزعهما عند النجاح. (2) أضف `tabindex="0"` مع `role="region"` و`aria-label` على الحاويتين. (3) أضف كتلة `@media (forced-colors: active)` تخفي `.editor-highlight` وتعيد `color: CanvasText` للـtextarea — يخسر التلوين ويكسب القراءة.

### 3.6 التغذية الراجعة للمستخدم — **6 / 10**

الموجود جيد: شريط حالة بثلاث حالات مع `aria-live` مُبدَّل بذكاء (`assertive` للخطأ و`polite` للنجاح — والتعليق يشرح: «polite كانت تؤجّل إعلان الخطأ»)، توست للنسخ، تعليم موضع الخطأ بخط متموّج تحت الرمز الجاني بالضبط، قائمة الدوال المدعومة تُعرض تلقائيًا عند استعمال دالة غير مدعومة، ونسخ بمسارين (Clipboard API ثم `execCommand`) مع `finally` ينظّف العقدة المؤقتة.

وميزة **التراجع عن المسح** بنافذة 8 ثوانٍ نمط ممتاز — أفضل من مربّع تأكيد لأنها ما تعترض الطريق.

**الخصم مركّز في مكانين:**
- **الفشل الصامت** (البند 3.2-أ) — أكبر ثقب تغذية راجعة في المنتج.
- **رسالة «الرجاء الإبلاغ عن هذه الصيغة»** (البند 2.4-د) تظهر لتجاوز حدّ موارد عادي، فتُقلق المستخدم وتدفعه لفتح بلاغ عن عطل غير موجود.

### متوسط فئة الـUX/UI

`(9 + 7 + 8 + 8 + 6 + 6) ÷ 6 = 44 ÷ 6 = ` **7.3 / 10**

---

## 4. المراجعة الأمنية — **8.6 / 10** (وزن 25%)

**تعديل المعيار حسب السياق:** المشروع أداة متصفح أوفلاين بلا خادم، بلا قاعدة بيانات، بلا حسابات، بلا شبكة، ولا تخزّن أي بيانات مستخدم (‏`localStorage` تحمل تفضيل الثيم فقط). لذا **الصلاحيات والمصادقة، والبيانات الحساسة، وCORS — لا تنطبق**، والدرجة محسوبة من النقاط المنطبقة الخمس فقط.

### 4.1 المفاتيح والأسرار — **10 / 10** · لا ملاحظات
`.gitignore` سليم، لا `.env` ولا مفاتيح في الشجرة، ولا شيء في `package.json`. المشروع بطبيعته لا يحتاج أي سرّ.

### 4.2 الحقن — **8 / 10**

**الإيجابي — والقوي:** `ui.js` **لا يستعمل `innerHTML` إطلاقًا**. كل شيء عقد DOM و`textContent`، وهذا مذكور كمبدأ معماري في رأس الملف. هذا يلغي فئة XSS كاملة من جذرها، لا يخفّفها. وحاولت الحقن عبر الصيغة نفسها (سلاسل باكتيك، `+process.exit()+`، اقتباسات مهرّبة) فما نفذ شيء — `JSON.stringify` يهرّب كل النصوص، وأسماء الخلايا مقيَّدة بنمط `[A-Za-z]+\d+`، وأسماء الدوال تُطابَق على سجلّ `FUNCTIONS` أو تُرفض. **لا مسار حقن داخل التطبيق.**

**🟠 الملاحظة — حقن في وجهة الكود المولّد لا في الأداة:**
`JSON.stringify` لا يهرّب `/`. **مُتحقَّق:** `="</script><img src=x>"` يُنتج كودًا يحتوي `</script>` حرفيًا:

```js
return "</script><img src=x>";
```

المستخدم اللي يلصق هذا الناتج داخل وسم `<script>` مضمّن في صفحته يُنهي السكربت مبكرًا ويحقن HTML في **صفحته هو**. المخاطرة على المستهلك النهائي لا على الأداة، لكن مولّد الكود مسؤول عن سلامة مخرجه.
**مُتحقَّق كذلك:** `U+2028`/`U+2029` يمرّان خامًا (‏غير ضارّين في محرّكات ES2019+ لكنهما يكسران التضمين في JSON أو الأدوات القديمة).
**الحل:** في `engine.js` حالة `'String'`، استبدل `JSON.stringify(node.value)` بـ:

```js
JSON.stringify(node.value)
  .replace(/</g, '\\x3C')
  .replace(/\u2028/g, '\\u2028')
  .replace(/\u2029/g, '\\u2029')
```

الناتج يبقى صحيحًا وظيفيًا وآمنًا للتضمين في أي سياق.

### 4.3 التحقق من المدخلات — **9 / 10**

خمسة حدود صريحة ومُختبَرة (`Limits: 6/6`): طول الإدخال 10,000 · عمق التداخل 64 · خلايا النطاق 1,000 · إجمالي الخلايا 2,000 · طول الناتج 200,000 — وكل حدّ برسالة عربية تقول للمستخدم وش يسوي. حراسة موارد نموذجية لأداة تعالج مدخلًا حرًّا.

**🟠 الثغرة الوحيدة:** حدّ العمق يُتجاوَز عبر مسار الـunary (البند 2.4-د) فينتهي بـ`RangeError` على المكدّس بدل رفض مهذّب. الأثر محصور في تبويب المستخدم نفسه (لا خادم يسقط)، فخطورته تشغيلية لا أمنية — لكنه يخرق الغرض المعلن من `LIMITS`.

### 4.4 الاعتماديات — **9 / 10**

**صفر تبعيات وقت تشغيل** — أقوى موقف أمني ممكن لسلسلة التوريد. تبعيتا تطوير فقط (`jsdom`, `prettier`)، و`dependabot.yml` يراقب npm وGitHub Actions أسبوعيًا، وإجراءات الـCI **مثبّتة على SHA** لا على وسوم قابلة لإعادة التوجيه — والتعليق يشرح المقايضة صراحةً. مهمة الـCI تعلن `permissions: contents: read` فلا ترث صلاحيات الكتابة الافتراضية. هذا أنضج من أغلب المستودعات بهذا الحجم.

### 4.5 الترويسات وCSP — **7 / 10**

CSP عبر `<meta>` (‏الخيار الوحيد المتاح على GitHub Pages) وهي مضبوطة بعناية:

```
default-src 'none'; script-src 'self' 'sha256-…' 'unsafe-eval';
style-src 'self' 'unsafe-inline'; img-src 'self' data:; base-uri 'none'; form-action 'none'
```

`default-src 'none'` يمنع كل استدعاء شبكي، و`base-uri`/`form-action` مقفلان، والسكربت المضمّن (تطبيق الثيم) مسموح بـ**hash** لا بـ`'unsafe-inline'` — مع اختبار يعيد حساب الـhash ويقارنه. و`referrer: no-referrer`.

**🟠 الملاحظة:** `'unsafe-eval'` موجودة في CSP الإنتاج لغرض واحد فقط — فحص نحوي في `engine.js`:

```js
// شبكة أمان: نتأكد أن التعبير المولّد صالح نحوياً قبل عرضه للمستخدم
try { new Function(paramNames.join(', '), `return ${expr};`); } catch (e) { … }
```

الفحص يلتقط أخطاء الـgenerator لا أخطاء المستخدم — أي إنه **حارس تطوير يُشحن للإنتاج**، وثمنه إضعاف أقوى بند في الـCSP. ومفارقة ثانية: نفس الحارس هو اللي يُنتج رسالة «الرجاء الإبلاغ» المضلِّلة في البند 2.4-د. الخطورة العملية اليوم منخفضة (لا `innerHTML`، لا شبكة، لا مدخل خارجي)، لكنها تلغي طبقة دفاع عن أي ثغرة مستقبلية.
**الحل الموصى به:** انقل الفحص خلف علم تطوير (‏`NS.DEV === true` أو `location.protocol === 'file:'`) واحذف `'unsafe-eval'` من `index.html`؛ الاختبارات في `tests/` تستعمل `new Function` أصلًا في Node وتحتفظ بالتغطية كاملة. **الفائدة الجانبية:** حذف الحارس يجعل تجاوز حدّ الـunary يظهر كخطأ `RangeError` واحد لا كرسالة «بلّغ عن عطل».

### متوسط الفئة الأمنية

`(10 + 8 + 9 + 9 + 7) ÷ 5 = 43 ÷ 5 = ` **8.6 / 10**

---

## 5. الدرجة الإجمالية المرجّحة

| الناحية | الدرجة | الوزن | المساهمة |
|---|---:|---:|---:|
| تقنية وهندسية | 8.0 / 10 | 45% | 3.60 |
| المظهر وتجربة المستخدم | 7.3 / 10 | 30% | 2.19 |
| أمن سيبراني | 8.6 / 10 | 25% | 2.15 |
| **الإجمالي** | | | **7.94 / 10** |

**الحساب:** `(8.0 × 0.45) + (7.3 × 0.30) + (8.6 × 0.25) = 3.60 + 2.19 + 2.15 = ` **7.94 ≈ 7.9 / 10**

**قراءة الدرجة:** المشروع فوق المتوسط بوضوح، وبناؤه وتوثيقه وموقفه الأمني في مستوى أعلى من حجمه. المصعد الوحيد نحو 9 هو إغلاق فجوات دلالات Excel — لأنها الفئة الوحيدة اللي تعطي المستخدم **إجابة خاطئة بلا إنذار**، وهذا أسوأ فشل ممكن لأداة تحويل: المستخدم يبني عليها ولا يعرف.

**مقارنة بالجولات السابقة:** 7.1 → 7.5 → **7.9**. الاتجاه صاعد وثابت، والمكاسب هذه المرة جاءت من عمق الهندسة لا من التلميع.

---

## 6. خطة الإصلاح المرتبة بالأولوية

| # | الأولوية | الناحية | المشكلة | الحل المقترح | التأثير المتوقع |
|---:|:---:|---|---|---|---|
| 1 | 🔴 حرج | هندسة | `=A1+A2` على `"5"` و`"3"` يعطي `"53"` بدل `8` — دمج نصّي بدل جمع، بلا أي إنذار | `helpers.js`: أضف `_add` (يحوّل الطرفين بـ`Number`، ويعيد `#VALUE!` عند `NaN`). `engine.js` في `gen()` حالة `Binary`: أضف `if (node.op === '+') { registerHelper('_add'); return \`_add(${L}, ${R})\`; }` قبل سطر `opMap` | ينتفي أخطر مصدر لإجابة خاطئة صامتة؛ يطابق سلوك `-`/`*`/`/` الصحيحة أصلًا |
| 2 | 🔴 حرج | هندسة | `TRIM` لا تطوي المسافات الداخلية: `" a   b "` → `"a   b"` بدل `"a b"` | `functions.js` في `TRIM`: `` `_str(${args[0]}).replace(/\s+/g, ' ').trim()` ``، وحدّث `jsEquiv` إلى `str.replace(/\s+/g,' ').trim()` ليطابق الجدول المرجعي الواقع | الغرض الأساسي من `TRIM` (تنظيف بيانات ملصوقة) يشتغل فعلًا |
| 3 | 🟠 مهم | هندسة | `REPLACE` بلا حراسة وسائط: `REPLACE("HELLO",-3,2,"X")` → `"HXLO"`، و`start=0` → `"HELLXELLO"` | `functions.js` في `REPLACE`: لفّ الناتج بـ`ctx.once(start, (st) => \`(${st} < 1 \|\| (${num}) < 0 ? '#VALUE!' : …)\`)` على نمط `MID` الموجود | يكتمل إصلاح v3.1.3 اللي شمل `LEFT`/`RIGHT`/`MID` وترك `REPLACE` |
| 4 | 🟠 مهم | هندسة / أمن | حدّ `parseDepth` لا يغطي `parseUnary` — سلسلة معاملات أحادية تكسر المكدّس وتعرض «الرجاء الإبلاغ عن هذه الصيغة» أو `RangeError` إنجليزيًا خامًا | `engine.js`: طبّق نفس حارس العمق داخل `parseUnary` (وحلقة `parsePostfix`)، أو أضف عدّادًا للمعاملات الأحادية المتتالية يرمي رسالة «الصيغة معقّدة جداً» المُموضَعة | يُغلق آخر مسار انهيار غير محكوم؛ تختفي رسالة تطلب من المستخدم بلاغًا عن عطل غير موجود |
| 5 | 🟠 مهم | UX | الفشل صامت والنجاح مسموع: لصق صيغة فيها دالة غير مدعومة يترك لوحة فاضية بلا تفسير ولا إشارة إلى زرّ «تحويل» | `ui.js` في `doConvert`: عند `silent` والفشل، اعرض في لوحة المخرَج «الصيغة غير مكتملة أو غير مدعومة — اضغط تحويل للتفاصيل»؛ أو ميّز اللصق (قفزة طول > 10 أحرف) واعرض الخطأ كاملًا | يختفي أكبر طريق مسدود في الرحلة الأساسية |
| 6 | 🟠 مهم | UX / وصول | لوحة الكود والجدول المرجعي قابلان للتمرير بلا `tabindex` — خرق WCAG 2.1.1 | `index.html`: أضف `tabindex="0"` و`role="region"` و`aria-label` على `#output-wrap` و`.ref-table-wrap` | مستخدم لوحة المفاتيح يقدر يقرأ الكود المولّد كاملًا |
| 7 | 🟠 مهم | UX / وصول | رسالة الخطأ غير مرتبطة بالحقل: لا `aria-invalid` ولا `aria-describedby` | `ui.js`: عند الخطأ `$input.setAttribute('aria-invalid','true')` و`aria-describedby="status"`، وانزعهما عند النجاح | قارئ الشاشة يعلن الخطأ وسببه عند التركيز على المحرر |
| 8 | 🟠 مهم | أمن | `'unsafe-eval'` في CSP الإنتاج لأجل فحص نحوي تطويري وحده | `engine.js`: اجعل حارس `new Function` خلف علم تطوير، ثم احذف `'unsafe-eval'` من وسم CSP في `index.html` (‏الاختبارات في Node تحتفظ بالتغطية) | يعود `script-src` مقفلًا بالكامل؛ ويحلّ نصف البند #4 مجانًا |
| 9 | 🟡 تحسين | أمن | `JSON.stringify` لا يهرّب `/` — نصّ فيه `</script>` يخرج حرفيًا فيكسر أي تضمين مضمّن عند المستخدم | `engine.js` حالة `'String'`: مرّر ناتج `JSON.stringify` بـ`.replace(/</g,'\\x3C')` وبتهريب `\u2028`/`\u2029` | الكود المولّد آمن للصق في أي سياق، بما فيه وسم `<script>` مضمّن |
| 10 | 🟡 تحسين | هندسة | الأخطاء #1–#3 كلها في فروع غير مغطّاة، ولا توجد أداة قياس تغطية | أضف اختبارات القيم الحدّية مع كل إصلاح (`+` بمدخلات نصّية · `TRIM` بمسافات داخلية · `REPLACE` بوسائط غير صالحة · سلسلة unary طويلة)، وأضف `c8` بعتبة `--lines 85` في `.github/workflows/tests.yml` | الفجوات تُكشف آليًا بدل القراءة اليدوية |
| 11 | 🟡 تحسين | توثيق | جدول «الانحرافات المعروفة» ينقصه ثلاثة انحرافات — والمشروع ملتزم سياسة توثيق كل انحراف | بعد إصلاح #1–#3 ينتفي السبب؛ ما تختار إبقاءه أضفه صفًّا في الجدول | التعاقد المعلن مع المستخدم يبقى صادقًا |
| 12 | 🟡 تحسين | UX | 120 باراميتر في التوقيع بلا إرشاد إلى مفتاح «النطاقات كمصفوفات» | `ui.js` في `showOutputInfo`: إذا `paramNames.length > 15` والمفتاح مطفأ، أضف «💡 عدد الباراميترات كبير — جرّب وضع النطاقات كمصفوفات» | الميزة الموجودة تُكتشف وقت الحاجة إليها |
| 13 | 🟡 تحسين | UX / وصول | لا `@media (forced-colors: active)` — حيلة النص الشفاف تُظهر نصًّا مزدوجًا في وضع التباين العالي | `index.html`: كتلة تخفي `.editor-highlight` وتعيد `color: CanvasText` للـ`.editor-input` | يخسر التلوين ويكسب القراءة لمستخدمي التباين العالي |
| 14 | 🟡 تحسين | UX | إخفاء عمود «الوصف» كليًا على الجوال يحذف أنفع الأعمدة | `index.html`: بدل `display:none`، اعرض الوصف تحت اسم الدالة في نفس الخلية على الجوال | الجدول المرجعي يبقى مفيدًا على الجوال |
| 15 | 🟡 تحسين | هندسة | أعداد الدوال في README مكتوبة يدويًا وتتقادم صامتة | اختبار يقارن أعداد README بـ`FUNCTIONS` (على غرار `UI: CSP guard`)، أو سكربت `npm run docs:counts` | التوثيق لا ينحرف عن الكود |
| 16 | 🟡 تحسين | هندسة | `generate()` بطول ~200 سطر، و`EXAMPLES` بيانات مدفونة داخل `initUI` | استخرج `gen` إلى دالة مستوى-وحدة تاخذ الحالة صراحةً؛ انقل `EXAMPLES` إلى أعلى `ui.js` أو ملف مستقل | قراءة أسهل وحدود مسؤولية أوضح |

---

## 7. حالة التنفيذ (v3.1.4)

الخطة نُفِّذت كاملة — 16 بندًا من 16. الأرقام أدناه تطابق ترقيم الجدول أعلاه.

| # | الحالة | ما تغيّر فعليًا |
|---:|:---:|---|
| 1 | ✅ | `helpers.js`: helper `_add` جديد · `engine.js`: `'+'` يمرّ عليه. `"5"+"3"` = `8`، والنص غير الرقمي `#VALUE!`، والفارغ صفر |
| 2 | ✅ | `functions.js`: `TRIM` صارت `replace(/\s+/g,' ').trim()` و`jsEquiv` تطابقها |
| 3 | ✅ | `functions.js`: `REPLACE` تحرس `start < 1` و`num < 0` بنمط `MID` |
| 4 | ✅ | `engine.js`: `parseUnary` صارت تكرارية لا تعاودية، و`parsePostfix` و`parseUnary` تحت `checkChain` بحدّ 64 — الرسالة عربية ومُموضَعة |
| 5 | ✅ | `ui.js`: تلميح في لوحة المخرَج عند الفشل الصامت + كشف اللصق بقفزة الطول فيُعرض الخطأ كاملًا |
| 6 | ✅ | `index.html`: `tabindex="0"` و`role="region"` و`aria-label` على `#output-wrap` و`.ref-table-wrap` |
| 7 | ✅ | `ui.js`: `setFieldError` تضبط `aria-invalid`/`aria-describedby` وتنزعهما عند النجاح |
| 8 | ✅ | `engine.js`: الفحص النحوي خلف `syntaxCheckEnabled()` (‏Node فقط، أو `ExcelToJS.SYNTAX_CHECK`) · `index.html`: `'unsafe-eval'` محذوفة من الـCSP |
| 9 | ✅ | `engine.js`: `jsString` تهرّب `</` و`U+2028`/`U+2029` |
| 10 | ✅ | 25 اختبارًا جديدًا (196 → 221) + `c8` بعتبات في `.c8rc.json` وخطوة في الـCI |
| 11 | ✅ | `README.md`: الثلاثة خرجت من جدول الانحرافات إلى قسم «صار مطابقًا»، ودخل مكانها انحراف `+` مع التاريخ |
| 12 | ✅ | `ui.js`: اقتراح وضع النطاقات فوق 15 باراميترًا |
| 13 | ✅ | `index.html`: كتلة `@media (forced-colors: active)` |
| 14 | ✅ | `index.html` + `ui.js`: الوصف ينتقل تحت اسم الدالة عبر `data-desc` بدل `display:none` |
| 15 | ✅ | `tests/run-node.js`: حارس يقارن أعداد فئات README بسجلّ `FUNCTIONS` |
| 16 | ✅ | `engine.js`: `gen` و`opMap` على مستوى الوحدة والحالة تصل عبر `state` — `generate()` من ~200 سطر إلى 127 · `ui.js`: `EXAMPLES` خارج `initUI` |

### انحراف واحد عن الحل المقترح — بند 9

التقرير اقترح تهريب كل `<` إلى `\x3C`. طبّقت `</` فقط، لأن تهريب `<` وحده يشوّه
شرطًا شائعًا جدًا: `=COUNTIF(A1:A10,"<5")` كان يخرج `"\x3C5"` بلا أي مكسب أمني —
الخطر الحقيقي في تتابع `</` وحده لأنه ما يُنهي وسم `<script>`. الاختبار
`"<" وحده يبقى بلا تهريب` يحرس هذا القرار.

### التحقّق

| الفحص | النتيجة |
|---|---|
| `node tests/run-node.js` | 221/221 ✓ |
| `TZ=America/New_York node tests/run-node.js` | 221/221 ✓ |
| `npx prettier --check` | نظيف ✓ |
| `npm run coverage` | أسطر 94.77% · فروع 87.74% · دوال 95.87% (فوق العتبات) |
| التحويل مع حجب `new Function` كليًا | نجح — إزالة `'unsafe-eval'` لا تكسر شيئًا ✓ |

### الدرجة بعد التنفيذ

الفئات المتأثّرة مباشرةً: معالجة الأخطاء والحالات الحدّية **5 → 9**، الاختبارات **7 → 9**،
التغذية الراجعة **6 → 8.5**، إمكانية الوصول **6 → 8.5**، الترويسات وCSP **7 → 9.5**،
الحقن **8 → 9.5**، التحقق من المدخلات **9 → 10**، التوثيق **9 → 9.5**، DRY **8 → 8.5**،
البنية المعمارية **9 → 9.5**، سهولة الاستخدام **7 → 8.5**، الاستجابة **8 → 8.5**.

- هندسة: `(9.5 + 9 + 8.5 + 9 + 8 + 9 + 9.5 + 9) ÷ 8 = ` **8.94**
- UX/UI: `(9 + 8.5 + 8.5 + 8 + 8.5 + 8.5) ÷ 6 = ` **8.5**
- أمن: `(10 + 9.5 + 10 + 9 + 9.5) ÷ 5 = ` **9.6**

`(8.94 × 0.45) + (8.5 × 0.30) + (9.6 × 0.25) = 4.02 + 2.55 + 2.40 = ` **8.97 ≈ 9.0 / 10** (من 7.9)

ما بقي خارج النطاق عمدًا: wildcards في `COUNTIF`، وحساسية الحالة في `<`/`>`،
والقسمة على صفر بعامل `/` — الثلاثة موثّقة في جدول الانحرافات وتغييرها قرار منتج
لا إصلاح خطأ. وتصيير الخط العربي على لينكس/أندرويد يبقى كما هو: حزم خط بـ`data:` URI
يضخّم الملف مئات الكيلوبايتات ولا يستحق.

---

## ملاحظة على نطاق المراجعة

قُرئت كل ملفات المصدر بالكامل (لا عيّنة)، وشُغّلت مجموعة الاختبارات فعليًا، وأُعيد إنتاج كل خطأ ورد أعلاه بتنفيذ الكود المولّد في Node ومقارنة الناتج بدلالات Excel. ما لم أستطع تقييمه من الكود وحده — **التصيير البصري الفعلي عبر المتصفحات، وسلوك قارئات الشاشة الحقيقية (NVDA/VoiceOver)، وتصيير الخط العربي على لينكس/أندرويد** — ذُكر كملاحظة مستنبطة من الكود ولم يُبنَ عليه حكم قاطع.
