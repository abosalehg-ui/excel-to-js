# 🔍 مراجعة شاملة — excel-to-js (V3) — الجولة الثانية

> ### ✅ حالة التنفيذ: خطة الإصلاح (القسم 6) نُفِّذت بالكامل — 22/22 بنداً
>
> هذا التقرير يبقى كما هو **بوصفه لقطة للحالة قبل الإصلاح** (النسخة 3.1.1)،
> ليكون سجلاً لما وُجد وكيف تم التحقق منه. الإصلاحات في النسخة 3.1.2.
> ملخص الأثر المقيس:
>
> | المقياس | قبل | بعد |
> |---|---|---|
> | نص المحرر | مزدوج ومشوّه، بلا تلوين | طبقة واحدة ملوّنة صحيحة |
> | ‏`'#N/A'` في الناتج | `&#39;#N/A&#39;` | `'#N/A'` |
> | تداخل `ISBLANK` بعمق 11 | 3.9 ميغابايت | 762 بايت |
> | Toast في الوضع الليلي | 1.20:1 | 15.28:1 |
> | أدنى تباين في الواجهة | 1.20:1 | 4.56:1 (كل العيّنات ≥ AA) |
> | عناصر قابلة للتنقل بالـTab | 13 | 33 |
> | تغطية `ui.js` | صفر اختبار | 27 اختباراً |
> | إجمالي الاختبارات | 123 | 172 |
> | ‏`tokenize` ×50 | 29ms | 17ms |
>
> راجع `README.md` للسلوك الحالي، وسجلّ الـcommits لتفاصيل كل إصلاح.

---


> مراجعة مستقلة بعد تنفيذ إصلاحات `REVIEW.md` (المراحل 0–4). كل ملاحظة هنا **تم التحقق منها بالتشغيل**:
> حزمة الاختبارات في Node، وتنفيذ `index.html` في Chromium على ثلاثة مقاسات (1280 فاتح، 1280 ليلي، 390 جوال)
> مع قياس `getComputedStyle` و contrast ratios رقمياً.
>
> **الدرجة الإجمالية: 7.1 / 10**

---

## 1. نظرة عامة سريعة

**ما يفعله المشروع:** أداة ويب ثابتة (static site) تحوّل صيغة Excel إلى دالة JavaScript جاهزة. البنية الداخلية بنية *مُصرِّف* كلاسيكية: `Tokenizer → Parser (Recursive Descent) → Code Generator`، مع سجلّ بيانات (`FUNCTIONS`) يصف كل دالة بعقدها (`minArgs/maxArgs/matrixArgs/usesHelpers`)، وسجلّ `HELPERS` يُولَّد عند الحاجة فقط بترتيب topological.

**التقنيات:** HTML5 + CSS3 (متغيرات CSS، Grid، media queries) + JavaScript ES2020 خام (`??`, `?.`, `flat(Infinity)`) بنمط UMD يدوي. **صفر تبعيات، صفر أدوات بناء، صفر backend.** الاختبارات بإطار محلي (`tests/framework.js`) يعمل في المتصفح وNode، وCI عبر GitHub Actions على منطقتين زمنيتين.

**حجم الكود:** 3416 سطراً — `index.html` 492، `engine.js` 459، `functions.js` 359، `ui.js` 305، `helpers.js` 233، `tests/suite.js` 678، `README.md` 291.

**الحكم المختصر:** المُصرِّف (engine + functions + helpers) عمل هندسي **قوي فعلاً** ومختبَر جيداً. الواجهة (`ui.js` + CSS) هي الحلقة الضعيفة — **صفر اختبارات**، وفيها ثلاثة أعطال بصرية مؤكدة تظهر لكل مستخدم في الشاشة الرئيسية.

---

## 2. المراجعة التقنية والهندسية — **7.3 / 10** (وزن 45%)

### 2.1 البنية المعمارية وفصل المسؤوليات — **8.5 / 10**

فصل المسؤوليات صحيح ونظيف. الاختبار الحقيقي لأي مُصرِّف صيغ هو حالتان يخطئ فيهما أغلب المنافسين، وكلاهما **صحيح هنا**:
- `engine.js:141-149` — `^` يساري الترابط: `2^3^2 = 64` (كما Excel، لا 512).
- `engine.js:150-157` — السالب الأحادي أقوى من `^`: `-2^2 = 4` (كما Excel، لا -4).
- `engine.js:159-166` — `%` لاحقة نسبة لا عامل ثنائي.

التصميم القائم على البيانات ممتاز: إضافة دالة جديدة = إدخال واحد في `functions.js` بلا لمس المحرّك، وفحص عدد الوسائط مركزي في `engine.js:339-351` بدل تكراره في 44 مكاناً.

**الخصم:** الاعتماد على `global` وترتيب `<script>` الضمني (`engine.js:10-12` يرمي استثناءً إن اختلّ الترتيب — تشخيص جيد لكن ليس حلاً). و`ui.js` سكربت عالي المستوى يستدعي `document.getElementById` عند سطر 10 مباشرة — أي أنه **غير قابل للاستيراد ولا للاختبار** خارج المتصفح، وهذا سبب مباشر لصفر تغطية الواجهة.

**الحل:** تحويل `ui.js` إلى `function initUI(root = document)` تُصدَّر وتُستدعى من سطر واحد في `index.html`. يفتح الباب لاختبار الواجهة بـ jsdom بلا تغيير المعمارية.

---

### 2.2 قابلية القراءة والصيانة — **8 / 10**

التعليقات العربية دقيقة و**تشرح "لماذا" لا "ماذا"** — وهذا نادر (مثال ممتاز: `functions.js:136` يشرح لماذا `slice(-0)` مشكلة، و`helpers.js:166-167` يشرح لماذا القسمة على 86400000 تكسر عند DST).

**الخصم — قوالب مولِّدة بسطر واحد ضخم:**
- `functions.js:191` — مولِّد `SUBSTITUTE` بـ **231 حرفاً في سطر واحد** يحوي `while` و`indexOf` ومنطق instance كامل. غير قابل للقراءة ولا للتنقيح.
- `functions.js:50` — `IFERROR` بنفس النمط (IIFE + closure + try/catch في سطر واحد).

**الحل:** استخدام template literals متعددة الأسطر (الكود المولّد يُعرض في محرر بأرقام أسطر أصلاً، فالأسطر المتعددة تحسّن الناتج أيضاً) — أو نقل المنطق المعقّد إلى `HELPERS` مثل بقية الدوال المركّبة. `SUBSTITUTE` تحديداً يجب أن تكون `_substitute` helper لا نص مضغوط.

**نقطة أسلوب:** `functions.js:192` — قوس إغلاق بمسافة بادئة خاطئة (6 مسافات بدل 4). لا يوجد `.editorconfig` ولا Prettier ولا ESLint في المشروع إطلاقاً، فمثل هذا لا يُلتقط آلياً.

---

### 2.3 التكرار في الكود (DRY) و Code Smells — **6 / 10**

#### 🔴 أخطر Code Smell في المشروع: تكرار تقييم الوسائط في الكود المولّد

عدة مولِّدات تُدخل تعبير الوسيط نفسه **أكثر من مرة** في الناتج:

| الموضع | التكرار |
|---|---|
| `functions.js:334` (`ISBLANK`) | `${args[0]}` **3 مرات** |
| `functions.js:340` (`ISNUMBER`) | `${args[0]}` مرتان |
| `functions.js:291,297,303` (`YEAR/MONTH/DAY`) | `${args[0]}` **3 مرات** |
| `functions.js:85` (`ROUND`) | `${digits}` مرتان |
| `functions.js:179` (`REPLACE`) | `${start}` مرتان |
| `functions.js:236,248` (`VLOOKUP/HLOOKUP`) | `${exact}` مرتان |

**الدليل (منفّذ فعلياً):**

```
>>> =ISBLANK(SUM(A1:A5))
return ([[a1,a2,a3,a4,a5]].flat(Infinity).reduce(...) === null
     || [[a1,a2,a3,a4,a5]].flat(Infinity).reduce(...) === undefined
     || [[a1,a2,a3,a4,a5]].flat(Infinity).reduce(...) === '');
```

`SUM` تُحسب **ثلاث مرات**. مع `ISBLANK(NOW())` تُستدعى `new Date()` ثلاث مرات بقيم مختلفة نظرياً.

**والأسوأ — التضخم أسّي (3ⁿ) عند التداخل.** قياس فعلي:

| عمق `ISBLANK` المتداخل | حجم الكود المولّد |
|---:|---:|
| 3 | 666 بايت |
| 6 | 16 KB |
| 9 | 430 KB |
| **11** | **3.9 MB** (خلال 122ms) |
| 13 | ~35 MB → تجميد التبويب |

المفارقة أن الاختبار `tests/suite.js:336` يفحص تحديداً أن `IFERROR` **لا** تكرر تعبيرها — الدرس طُبِّق على دالة واحدة فقط ولم يُعمَّم.

**الحل:** إضافة أداة مشتركة في المولّد تُلزم التقييم مرة واحدة، مثلاً `once(expr, body)` تُنتج `(_v => body)(expr)`:
```js
ISBLANK: { generator: ([v]) => `((_v)=>(_v===null||_v===undefined||_v===''))(${v})` }
```
ثم إضافة اختبار عام يفحص أن أي وسيط لا يظهر أكثر من مرة في ناتج أي دالة.

#### 🟠 تكرار سلسلة الترشيح الرقمي — 4 نسخ

`functions.js:65, 71, 77, 200` — `[...].flat(Infinity).filter(v=>typeof v==='number'&&!isNaN(v))` مكرّرة حرفياً في `AVERAGE`, `MIN`, `MAX`, `COUNT`.
**الحل:** helper واحد `_nums(arr)` في `helpers.js` (نفس نمط باقي الـ helpers، وسيقلّل حجم الكود المولّد أيضاً).

#### 🟡 قائمة الفئات مكرّرة في ثلاثة مواضع
`ui.js:28-31` (`CAT_LABELS`) + `index.html:273-279` (ألوان `.ref-cat`) + `index.html:384-390` (نسخة الوضع الليلي). إضافة فئة جديدة تتطلب تعديل 3 أماكن.

---

### 2.4 معالجة الأخطاء والحالات الحدّية — **6.5 / 10**

**القوة (حقيقية):** كل خطأ يحمل `start/end` ويُظلَّل في المحرر بدقة الحرف (`engine.js:54, 73, 97, 348`). عقود الوسائط المركزية (`engine.js:339-351`) تمنع توليد كود غير صالح. وشبكة الأمان في `engine.js:425` تتحقق نحوياً من الناتج قبل عرضه. هذه ممارسات أعلى من متوسط أدوات مشابهة.

**الثغرات المؤكدة بالتشغيل:**

| # | الحالة | المُدخل | الناتج الفعلي | المتوقع |
|---|---|---|---|---|
| E1 | مسافة/سطر قبل `=` | `" =A1"` | ❌ `رمز غير متوقع: "="` | يعمل عادي |
| E2 | تداخل عميق | `((((…20000…))))` | ❌ `Maximum call stack size exceeded` (إنجليزي خام) | رسالة عربية "الصيغة معقّدة جداً" |
| E3 | `MOD` بسالب | `=MOD(-3,2)` | `-1` | Excel: `1` |
| E4 | `ROUND` بسالب | `=ROUND(-2.5,0)` | `-2` | Excel: `-3` (تقريب بعيداً عن الصفر) |
| E5 | قسمة على صفر | `=1/A1`, a1=0 | `Infinity` | Excel: `#DIV/0!` |
| E6 | جذر سالب | `=SQRT(-1)` | `NaN` | Excel: `#NUM!` |
| E7 | منطقية داخل نطاق | `=SUM(A1:A2)`, a1=`true`,a2=5 | `6` | Excel: `5` (يتجاهل المنطقية) |

**E1 هو الأخطر عملياً:** المستخدم ينسخ صيغة من Excel وقد يلتقط مسافة أو سطراً. السبب: `engine.js:22` يستخدم `input.startsWith('=')` بدل `input.trim()`.
**الحل:** `src = input.trim()` مع تعديل `offset` بمقدار المسافات المحذوفة (لبقاء مواضع الأخطاء صحيحة). نفس التعديل في `ui.js:56`.

**E2:** لا يوجد حدّ لعمق التداخل في `parse()`.
**الحل:** عدّاد عمق في `parsePrimary` يرمي خطأً عربياً موضعياً عند تجاوز ~64.

**E3–E7:** انحرافات دلالية حقيقية **غير مذكورة** في جدول "الانحرافات المعروفة" بالـ README (`README.md:272-280`). هذا أخطر من الانحراف نفسه — الجدول يوحي بالاكتمال.
**الحل:** إصلاح `MOD` (`((a%b)+b)%b`) و`ROUND` (تقريب بعيداً عن الصفر) لأنهما رخيصان وشائعان جداً، وتوثيق E5–E7 صراحة.

**ثغرة صامتة:** `helpers.js:147` — `_countifs` يعتمد `ranges[0].length` فقط؛ إذا اختلفت أطوال النطاقات (`COUNTIFS(A1:A10,">0",B1:B5,"x")`) تُقرأ `undefined` بصمت بدل `#VALUE!` كما Excel.

---

### 2.5 الأداء واستهلاك الموارد — **5.5 / 10**

أضعف محور هندسي. ثلاث مشاكل مقيسة:

**P1 🔴 — التضخم الأسّي (مذكور في 2.3).** ليس نظرياً: عمق 11 = 3.9 MB. وبعده `ui.js:108` يمرّر الناتج على `escapeHtml` + **4 عمليات regex متتالية** على سلسلة بميغابايتات، ثم يبني `<div>` لكل سطر ويحقنه بـ `innerHTML` → **تجميد كامل للتبويب**. مسار الفشل: مُدخل صغير (~60 حرفاً) → تعليق المتصفح.

**P2 🟠 — Tokenizer بتعقيد O(n²).** `engine.js:46` — `src.slice(pos).match(regex)` يُنشئ سلسلة جديدة **لكل نمط ولكل token** (13 نمطاً × كل token). قياس: 50 دورة على صيغة 1897 حرفاً = 29ms.
**الحل:** regex بعلم `y` (sticky) مع `re.lastIndex = pos` — تعديل ميكانيكي على 13 سطراً يلغي التخصيص كلياً.

**P3 🟠 — إعادة تظليل بلا debounce.** `ui.js:194` — كل ضغطة مفتاح تعيد `tokenize()` كاملة + تبني سلسلة HTML كاملة + تحقن `innerHTML` (يهدم ويعيد بناء شجرة DOM). محسوس على الصيغ الطويلة.
**الحل:** `requestAnimationFrame` أو debounce 60–100ms.

**P4 🟡 — لا حدّ إجمالي للصيغة.** `engine.js:266` يحدّ كل نطاق بـ1000 خلية، لكن لا شيء يحدّ المجموع: `SUM(A1:A1000, B1:B1000, …)` بثمانية نطاقات → 8000 باراميتر و141 KB كود، دالة `calculate(a1, …, h1000)` غير قابلة للاستخدام أصلاً.
**الحل:** حدّ إجمالي (مثلاً 2000 خلية للصيغة كلها) + وضع إخراج بديل يستقبل كائناً واحداً `calculate(cells)` عند تجاوز 20 خلية (مذكور أصلاً كـ L11 في المراجعة السابقة، لم يُنفَّذ).

---

### 2.6 الاختبارات والتغطية — **8 / 10**

**123/123 ناجحة** (تحققت بالتشغيل، ~10ms). نقاط قوة حقيقية:
- فئة `Runtime` (66 اختباراً) تبني `calculate()` وتشغّلها بقيم فعلية عبر `new Function` (`tests/framework.js:65-72`) — اختبار end-to-end حقيقي لا مقارنة نصوص.
- CI يشغّل الحزمة **مرتين** بمنطقتين زمنيتين (`.github/workflows/tests.yml:15-21`) لالتقاط أخطاء DST — تفصيلة ناضجة جداً.
- `tests/run-node.js:38-51` يفحص أن شارات README تطابق الكود فعلياً — حماية ذكية من توثيق متعفّن.

**الفجوة الحاسمة — `ui.js` (305 سطراً) تغطيته 0%.** كل الأعطال البصرية في القسم 3 كانت ستُلتقط باختبار DOM واحد. الفجوة ليست عرضية: `ui.js` غير قابل للاستيراد (انظر 2.1)، فالتغطية مستحيلة بنيوياً لا مهملة.

**فجوات أخرى:** لا قياس تغطية إطلاقاً (لا نعرف نسبة تغطية `engine.js` نفسه)، ولا اختبارات property/fuzz — وهي الأنسب لمُصرِّف (توليد صيغ عشوائية والتأكد أن الناتج يُصرَّف دائماً بلا استثناء).

**الحل:** (أ) `initUI()` قابلة للاستيراد + اختبارات jsdom للتظليل والنسخ والتصفية؛ (ب) اختبار fuzz بسيط: 1000 صيغة مولّدة عشوائياً، لا شيء يرمي غير أخطاء مُعرّفة تحمل `start/end`.

---

### 2.7 التوثيق — **8.5 / 10**

`README.md` أعلى من المتوسط بوضوح: مخطط معماري نصي، شرح القرارات ("لماذا `matrixArgs`")، خارطة طريق مرحلية، و**جدول انحرافات عن Excel** (`README.md:272-280`) — نضج نادر.

**الخصم — ادعاءات غير دقيقة:**
1. `README.md:25` — «تظليل تركيبي — تلوين الصيغة أثناء الكتابة». **الميزة لا تظهر للمستخدم** (انظر 3.1). ادعاء لميزة معطّلة.
2. `README.md:272-280` — الجدول ناقص: `MOD` بسالب، `ROUND` بسالب، `#DIV/0!`، `#NUM!`، والمنطقية في `SUM` (E3–E7 أعلاه) كلها غائبة.
3. لا يوجد `CONTRIBUTING.md` ولا `.gitignore` ولا قالب PR رغم أن المشروع يستقبل PRs.

---

### 2.8 اتفاقيات التسمية ونمط الكود — **8 / 10**

الاتساق جيد: `_camelCase` للـ helpers الداخلية، `UPPER_CASE` للسجلّات، `$` للعناصر DOM، UMD موحّد في الملفات الثلاثة.

**الخصم:** **لا يوجد أي أداة تنسيق أو فحص** — لا ESLint، لا Prettier، لا `.editorconfig`، لا `package.json`. الاتساق الحالي بشري بالكامل وسينهار مع أول مساهم خارجي. الدليل موجود أصلاً: `functions.js:192` مسافة بادئة خاطئة، و`ui.js:256` (`${name}` بلا `escapeHtml`) بينما كل الحقول المجاورة مهرّبة.

**الحل:** `package.json` بـ `devDependencies` واحدة (Prettier) + خطوة في CI. لا يكسر مبدأ "صفر تبعيات" لأنها تبعية تطوير لا تشغيل.

---

## 3. المظهر العام والتصميم وتجربة المستخدم — **6.0 / 10** (وزن 30%)

> كل ما في هذا القسم مُتحقَّق منه بالتنفيذ في Chromium ولقطات شاشة فعلية، وقيم التباين محسوبة رقمياً بمعادلة WCAG.

### 3.1 🔴 العطل الأخطر: المحرر يعرض نصاً مزدوجاً مشوّهاً

**هذا ليس ملاحظة تجميلية — إنه أول ما يراه المستخدم.**

```
المعروض فعلياً في المحرر:
=SUM(A1:A5)+VLOOKUP(B2,C1:D9,2,FALSE)   ← نص الـ textarea (رمادي، محاذاة يسار)
              =SUM(A1:A5)+VLOOKUP(B2,C1:D9,2,FALSE)  ← طبقة التظليل (ملوّنة، محاذاة يمين)
                          ↑ الطبقتان تتراكبان = نص شبحي مضاعف غير مقروء
```

**السبب الجذري (مقيس بـ `getComputedStyle`):**

| العنصر | `text-align` المحسوب |
|---|---|
| `.editor-wrap` (`index.html:112`) | `right` |
| `.editor-highlight` (يرث) | `right` ✅ |
| `.editor-input` (textarea) | **`start`** ❌ |

متصفحات Chromium تفرض `text-align: start` على `<textarea>` من الـ UA stylesheet، فهو **لا يرث** `right` من الأب. النتيجة: الطبقتان لا تتحاذيان أبداً.

يضاعف المشكلة أن `index.html:124` يعطي الـ textarea `color: var(--text)` **معتماً** فوق طبقة التظليل (`z-index: 2` مقابل `1`). في النمط الصحيح لهذا التصميم يجب أن يكون نص الـ textarea **شفافاً** ليظهر التلوين من تحته. النتيجة المجتمعة: **التلوين التركيبي لا يعمل إطلاقاً** + نص مزدوج.

**الحل (3 أسطر CSS):**
```css
.editor-wrap  { direction: ltr; text-align: left; }   /* بدل right */
.editor-input { color: transparent; caret-color: var(--primary); text-align: left; }
.editor-highlight { color: var(--text); }             /* بدل transparent */
```
> ملاحظة: محاذاة اليمين لمحرر كود بـ `direction: ltr` قرار تصميمي مشكوك فيه أصلاً — الكود يُقرأ من اليسار حتى داخل صفحة RTL.

---

### 3.2 🔴 العطل الثاني: علامات الاقتباس تظهر كـ `&#39;` في الكود الناتج

مرئي في اللقطة على السطر 10 من الناتج:
```
return &#39;#N/A&#39;;      ← المفترض:  return '#N/A';
if (typeof a === &#39;string&#39;)
```

**السبب الدقيق** — `ui.js:81-98`، تسلسل عمليات `highlightJS`:

1. `escapeHtml(code)` (`ui.js:33-36`) يحوّل `'` → `&#39;`
2. `ui.js:92` — `result.replace(/\b(\d+\.?\d*)\b/g, '<span class="code-number">$1</span>')` يطابق **الرقم `39` داخل الكيان `&#39;`** ويشطره:
   ```
   &#<span class="code-number">39</span>;
   ```
3. الكيان مكسور → المتصفح يعرضه حرفياً.

**مفارقة مهمة:** هذا **انحدار (regression)** أدخله commit `f5a6f61` استجابةً للملاحظة L10 في المراجعة السابقة ("`escapeHtml` لا تهرّب علامات الاقتباس"). التهريب كان صحيحاً، لكن regex الأرقام لم يُحدَّث ليتجاهل الكيانات — ولا اختبار واجهة واحد التقط ذلك.

**الحل:** إما تحصين regex الأرقام بلوكهيد سلبي `(?<!&#)\b(\d+\.?\d*)\b`، أو (الأفضل جذرياً) استبدال سلسلة الـ regex بـ tokenizer عرض صغير يبني عقد DOM بـ `textContent` بدل `innerHTML` — يلغي فئة الأخطاء هذه بالكامل ويلغي أيضاً مشكلة الـ spans المتداخلة.

---

### 3.3 🔴 العطل الثالث: الـ Toast غير مرئي في الوضع الليلي

`index.html:286-295` — `.toast { background: var(--text); color: white; }`

في الوضع الليلي `--text: #e8eaf0` (شبه أبيض) ← نص أبيض على خلفية شبه بيضاء.

**نسبة التباين المقيسة: 1.20 : 1** (الحد الأدنى المطلوب 4.5). رسالة "تم النسخ ✓" **غير مقروءة عملياً** — مؤكَّد بلقطة شاشة. وهي التغذية الراجعة الوحيدة لأهم إجراء في الأداة (النسخ).

**الحل:** `.toast { background: var(--surface); color: var(--text); border: 1px solid var(--border-strong); }` أو متغير مخصص `--toast-bg` بقيمتين.

---

### 3.4 الاتساق البصري — **6 / 10**

نظام التصميم نفسه جيد: متغيرات CSS منظمة، مقياس مسافات متسق (8/12/14/16/18/24/32)، نصف أقطار موحّدة.

**لكن ألوان التظليل التركيبي بلا نسخة ليلية إطلاقاً.** `index.html:133-138` قيم ثابتة مصمَّمة للخلفية الفاتحة، تُستخدم كما هي فوق `--surface: #1a1d27`:

| الفئة | اللون | التباين على السطح الليلي |
|---|---|---:|
| `.tk-paren` | `#2d3748` | **1.40** 🔴 |
| `.tk-op` | `#4a5568` | **2.23** 🔴 |
| `.tk-cell` | `#6b46c1` | **2.62** 🔴 |
| `.tk-fn` | `#2b6cb0` | **3.10** 🔴 |
| `.tk-str` | `#2f7d4f` | **3.34** 🔴 |

الأقواس والعمليات تقارب الاختفاء التام. لاحظ أن بقية النظام **أُعطي** نسخاً ليلية بعناية (`.ref-cat` في `index.html:384-390`، `.status` في `198-200`) — التظليل وحده نُسي، على الأرجح لأنه غير مرئي أصلاً بسبب 3.1، فالعطل الأول أخفى الثاني.

---

### 3.5 سهولة الاستخدام ووضوح التنقل — **7.5 / 10**

بنية الصفحة منطقية: عنوان → حالة → محرر/ناتج جنباً إلى جنب → زر تحويل → أمثلة → مرجع مصنّف. الأمثلة الـ18 مدخل ممتاز للمستخدم الجديد، واختصار `Ctrl+Enter` معروض بجانب الزر (`index.html:454`).

**ملاحظات:**
- لا تحويل تلقائي ولا معاينة حية — التحويل فوري (3ms) فلا مبرر لإلزام المستخدم بالضغط. **الحل:** تحويل تلقائي بـ debounce 300ms مع إبقاء الزر للوضوح.
- `ui.js:187-193` — زر "مسح" يمحو بلا تأكيد ولا تراجع.
- رسالة النجاح لا تختفي أبداً، وتبقى معروضة فوق ناتج قديم بعد أن يبدأ المستخدم بالتعديل.
- الجدول المرجعي (44 صفاً) بلا بحث نصي — التصفية بالفئة فقط.

---

### 3.6 الاستجابة عبر أحجام الشاشات — **6.5 / 10**

عمل جوال حقيقي موجود: شريط تحويل ثابت أسفل الشاشة (`index.html:341-352`)، جدول قابل للتمرير أفقياً، إخفاء عمود الوصف، تمرير أفقي لأزرار التصفية.

**المشاكل (مؤكدة بلقطة 390×844):**
1. **التعليق يناقض الكود:** `index.html:360` مكتوب `/* الـ examples: عمود واحد */` بينما `index.html:361` يضع `grid-template-columns: 1fr 1fr` — **عمودان**. النتيجة على 390px: بطاقات بعرض ~180px وخط 11px، وصيغة مثل `=COUNTIFS(A1:A10,">0",B1:B10,"معتمد")` تنكسر على 3 أسطر بـ`word-break: break-all` وتصبح غير مقروءة. **الحل:** `1fr` فعلاً كما ينص التعليق.
2. **فجوة 641–900px** (لوحي عمودي): `.main-grid` يبقى عمودين حتى 900px، فيصبح كل لوح ~330px والكود الناتج يُقرأ في نافذة ضيقة جداً. **الحل:** رفع نقطة الكسر إلى 1024px.
3. لا يوجد `@media (prefers-reduced-motion)` رغم وجود انتقالات وtransform في `.example-card` و`.toast`.

---

### 3.7 دعم العربية و RTL — **8 / 10**

الأفضل بين المحاور. `<html lang="ar" dir="rtl">` صحيح، والكود يُعزل في جزر LTR (`.output-wrap`, `.js-equiv`, `.formula`, `.editor-wrap`) — الأسلوب الصحيح تماماً. `.status { border-right: 4px }` مناسب لـRTL، ورؤوس الجداول محاذاة يمين.

**الخصم:**
- **خصائص فيزيائية بدل منطقية:** `index.html:191` (`border-right`)، `index.html:90` (`margin-left`)، `index.html:150` (`border-left`). تكسر فوراً عند إضافة نسخة إنجليزية. **الحل:** `border-inline-start` / `margin-inline-start`.
- **لا خط عربي مخصص:** `index.html:74` — `'Segoe UI', Tahoma, sans-serif`. على لينكس/أندرويد لا يوجد أي منهما، فيسقط على خط النظام الافتراضي (النتيجة مرئية في اللقطات: تصيير عربي باهت). **الحل:** `font-family: 'Segoe UI', 'Noto Sans Arabic', Tahoma, system-ui, sans-serif` — بلا تحميل خارجي، يحافظ على مبدأ "أوفلاين بالكامل".
- محاذاة المحرر يميناً (انظر 3.1) — قرار RTL طُبِّق في مكان لا يناسبه.

---

### 3.8 إمكانية الوصول (Accessibility) — **4 / 10**

أضعف محور في المشروع كله. المراجعة السابقة (L6) أضافت `aria-label` و`aria-pressed` و`role="status"` — وهذا جيد — لكن الأساسيات ما زالت مفقودة.

**A1 🔴 — 18 بطاقة مثال غير قابلة للوصول بلوحة المفاتيح.**
`ui.js:229-247` — كل بطاقة `<div>` بمستمع `click` بلا `tabindex` ولا `role` ولا معالج `keydown`.
**مقيس فعلياً في المتصفح:** `document.querySelectorAll('.example-card[tabindex]').length === 0` مقابل 18 بطاقة، وإجمالي العناصر القابلة للتنقل بالـTab = 13 فقط.
مستخدم لوحة المفاتيح أو قارئ الشاشة **لا يستطيع استخدام الأمثلة إطلاقاً**.
**الحل:** `<button type="button" class="example-card">` — يحل التنقل والدلالة والتفعيل بـEnter/Space دفعة واحدة، بلا JS إضافي.

**A2 🔴 — إزالة مؤشر التركيز من المحرر.**
`index.html:125` — `outline: none;` على `.editor-input`، والبديل `background: rgba(49,130,206,0.02)` (`index.html:127`) شفافية 2% أي **غير مرئي عملياً**. مخالفة صريحة لـWCAG 2.4.7.
**الحل:** `.editor-input:focus-visible { outline: 2px solid var(--accent); outline-offset: -2px; }`

**A3 🟠 — الـToast لا يُعلَن لقارئات الشاشة.**
`index.html:485` بلا `role="status"` (مؤكد: `getAttribute('role') === null`). مستخدم قارئ الشاشة لا يعرف أن النسخ تم.
**الحل:** `role="status" aria-live="polite"`.

**A4 🟠 — لا يوجد landmark رئيسي.**
`index.html` بلا `<main>` إطلاقاً (مؤكد: `document.querySelector('main') === null`) — مفارقة أن `tests.html:108` **فيه** `<main>`. ولا رابط تخطٍّ للمحتوى.

**A5 🟠 — إخفاقات تباين (محسوبة):**

| الموضع | الألوان | النسبة | الحكم |
|---|---|---:|---|
| Footer (`index.html:284`) | `#8a94a3` على `#f4f6f9` | **2.83** | 🔴 فشل |
| أرقام الأسطر (`index.html:148`) | `#475569` على `#1e293b` | **1.93** | 🔴 فشل |
| `--text-mute` ليلي | `#5a6278` على `#0f1117` | **3.10** | 🔴 فشل |
| Toast ليلي (3.3) | `#ffffff` على `#e8eaf0` | **1.20** | 🔴 فشل حرج |
| `.tk-*` ليلي (3.4) | — | 1.40–3.34 | 🔴 فشل |
| نص أساسي `--text-soft` | `#5a6677` على `#f4f6f9` | 5.39 | ✅ |
| زر أساسي | `#ffffff` على `#2c5282` | 7.97 | ✅ |

**A6 🟡** — لا `<noscript>`: بلا JavaScript تظهر صفحة شبه فارغة (الأمثلة والجدول والعدّادات كلها مبنية بـJS) بلا أي تفسير.

---

### 3.9 التغذية الراجعة للمستخدم — **6.5 / 10**

**القوة الحقيقية:** تحديد موضع الخطأ بدقة الحرف (`ui.js:152-153` + `.err-mark`) — تجربة مطوّر ممتازة نادرة في أدوات بهذا الحجم. ورسائل الخطأ عربية وموجّهة للحل (`engine.js:54` يقترح البديل الصحيح: `"استخدم شكل مثل B1:C100"`).

**النواقص:** لا حالات تحميل (مقبول — التحويل 3ms)؛ الـtoast معطّل بصرياً في الليلي وغير معلَن لقارئ الشاشة؛ لا تأكيد على المسح؛ الخطأ الوحيد غير المُعرَّب هو `Maximum call stack size exceeded` (E2)؛ ورسالة "الدوال المدعومة" (`ui.js:157`) تسرد 44 اسماً في سطر واحد بلا اقتراح "هل تقصد…؟".

---

## 4. المراجعة الأمنية — **8.2 / 10** (وزن 25%)

> **سياق:** موقع ثابت بلا backend وبلا شبكة وبلا حالة مستخدم. سطح الهجوم ضيق أصلاً، والدرجة تعكس ذلك بصدق. النموذج التهديدي الواقعي الوحيد: مستخدم يفتح رابطاً/صيغة أعدّها طرف آخر.

### 4.1 تخزين المفاتيح والأسرار — **10 / 10** ✅

فحص كامل للريبو: **صفر أسرار**. لا مفاتيح، لا رموز، لا `.env`، لا نداءات شبكة، لا أصول خارجية في وقت التشغيل (`index.html` و`tests.html` لا يحمّلان أي URL خارجي). التخزين المحلي الوحيد `ui.js:290` = تفضيل الثيم — بيانات غير حساسة إطلاقاً.

### 4.2 ثغرات الحقن (XSS / Code Injection) — **8 / 10** 🟡 بسيط

**لم أجد XSS قابلاً للاستغلال.** الدفاعات صحيحة في مسارها الحرج:
- `ui.js:33-36` — `escapeHtml` تهرّب `& < > " '` وتُطبَّق على كل مُدخلات المستخدم ورسائل الأخطاء (`ui.js:155`).
- **حقن الكود المولّد مغلق:** نصوص المستخدم تمر عبر `JSON.stringify` (`engine.js:300`)، وأسماء الخلايا مقيّدة بـregex صارم `/^\$?[A-Za-z]+\$?\d+/` ثم `.replace(/\$/g,'')` — لا مسار لإدخال محارف تنفيذية.

**الخطورة: بسيط — النمط هشّ لا مخترَق.**
`ui.js:81-98` — `highlightJS` تُجري **جراحة regex على HTML مُهرَّب مسبقاً**. هذا نمط معروف بهشاشته، و**قد أنتج عطلاً فعلياً بالفعل** (كسر الكيانات، القسم 3.2). اليوم غير مستغَل لأن كل `<` و`>` مُهرَّبة قبل أي استبدال — لكنه على بُعد تعديل واحد من أن يصبح ثغرة.
**الحل:** بناء عقد DOM بـ`textContent` بدل `innerHTML` — يغلق فئة الخطر ويصلح عطل 3.2 معاً.

**الخطورة: بسيط — `new Function`.**
`engine.js:425` — يُستخدم كشبكة أمان نحوية. **صحيح أنه يُصرِّف ولا ينفّذ**، والمدخلات مقيّدة كما أعلاه. لكن وجوده يمنع تطبيق CSP صارم (انظر 4.7). ملاحظة منفصلة: `tests/framework.js:65-72` **ينفّذ** الكود فعلاً — مقبول لأنه يعمل على صيغ ثابتة من الحزمة فقط، لا على مُدخل مستخدم.

**نقطة اتساق:** `ui.js:256` — `${name}` يُحقن بلا `escapeHtml` بينما `jsEquiv` و`desc` المجاوران مهرَّبان. آمن اليوم (المصدر ثابت داخلي) لكنه شذوذ يجب إغلاقه دفاعياً.

### 4.3 التحقق من المدخلات — **7 / 10** 🟡 متوسط

التحقق **إيجابي (allow-list)** وهو النهج الصحيح: الـtokenizer يقبل أنماطاً محددة ويرفض ما عداها (`engine.js:72-77`)، والـparser يرفض الرموز الزائدة (`engine.js:221-226`)، وعقود الوسائط تُفحص مركزياً (`engine.js:339-351`)، وحد 1000 خلية لكل نطاق (`engine.js:266`).

**الناقص — حدود الموارد (متوسط، توفّر خدمة):**
| # | الفجوة | الأثر |
|---|---|---|
| V1 | لا حدّ لعمق التداخل | `RangeError` (مُلتقَط، لكن رسالة غير مُعرَّبة) |
| V2 | لا حدّ لحجم الكود المولّد | تجميد التبويب — صيغة 60 حرفاً → 3.9 MB (القسم 2.5) |
| V3 | لا حدّ إجمالي للخلايا | 8 نطاقات → 8000 باراميتر |
| V4 | لا حدّ لطول المُدخل | Tokenizer O(n²) على لصق ضخم |

**سيناريو الاستغلال الواقعي:** مشاركة رابط/صيغة معدّة تجمّد متصفح الضحية. الأثر محدود بتبويب واحد (client-side DoS)، لكنه حقيقي ورخيص الإصلاح.
**الحل:** 4 حراس رقمية: طول المُدخل ≤ 10000، عمق التداخل ≤ 64، إجمالي الخلايا ≤ 2000، وفحص `expr.length` قبل العرض.

### 4.4 المصادقة والصلاحيات — **غير منطبق** ✅

لا مستخدمين، لا جلسات، لا صلاحيات، لا بيانات مشتركة. **الغياب هنا هو التصميم الصحيح** — لا يوجد ما يُحمى. لم أُدرجها في حساب المتوسط.

### 4.5 التعامل مع البيانات الحساسة — **9 / 10** ✅

لا بيانات حساسة تُعالَج أو تُرسَل أو تُخزَّن. صيغة المستخدم لا تغادر المتصفح إطلاقاً (لا `fetch`، لا `XHR`، لا تتبّع، لا تحليلات) — وهذا **ميزة خصوصية حقيقية** يستحق المشروع الإشادة عليها ويستحق ذكرها صراحة في README كنقطة بيع.
النقل عبر HTTPS مؤمَّن افتراضياً على GitHub Pages.

### 4.6 الاعتماديات — **10 / 10** ✅

**صفر تبعيات وقت تشغيل.** لا `package.json`، لا `node_modules`، لا CDN. سطح هجوم سلسلة التوريد **معدوم** — أقوى قرار أمني في المشروع.

**ملاحظة بسيطة على CI:** `.github/workflows/tests.yml:11-13` يستخدم `actions/checkout@v4` و`actions/setup-node@v4` بوسم رئيسي لا بـSHA. المخاطرة نظرية (إجراءات رسمية من GitHub) لكن التثبيت بـSHA هو الممارسة الموصى بها.

### 4.7 إعدادات CORS و Headers الأمنية — **5 / 10** 🟡 متوسط

أضعف محور أمني.

**S1 🟡 — لا CSP إطلاقاً.** لا `<meta http-equiv="Content-Security-Policy">` في `index.html`. GitHub Pages لا يسمح بترويسات مخصصة، لكن وسم meta **متاح ومجاني**. الاعتراض المشروع: `engine.js:425` يستخدم `new Function` فيتطلب `'unsafe-eval'`.
**الحل المتدرّج:** (أ) جعل شبكة الأمان النحوية اختيارية (متغير تهيئة) أو استبدالها بفحص توازن أقواس بسيط؛ (ب) ثم تطبيق:
```html
<meta http-equiv="Content-Security-Policy"
      content="default-src 'none'; script-src 'self' 'unsafe-inline';
               style-src 'self' 'unsafe-inline'; img-src 'self' data:; base-uri 'none'">
```
حتى مع `'unsafe-inline'` مؤقتاً، `default-src 'none'` وحدها تمنع أي تسريب بيانات أو تحميل خارجي.

**S2 🟡 — لا `permissions` في workflow.** `.github/workflows/tests.yml` بلا كتلة صلاحيات → `GITHUB_TOKEN` يرث الافتراضي على مستوى الريبو (قد يكون read-write). المهمة تحتاج قراءة فقط.
**الحل:**
```yaml
permissions:
  contents: read
```

**S3 🟡 — لا `<meta name="referrer">`.** موصى به لموقع ثابت: `<meta name="referrer" content="no-referrer">`.

---

## 5. الدرجة الإجمالية المرجّحة

| الناحية | الدرجة | الوزن | المساهمة |
|---|---|---|---|
| تقنية وهندسية | 7.3/10 | 45% | 3.29 |
| UX/UI | 6.0/10 | 30% | 1.80 |
| أمن سيبراني | 8.2/10 | 25% | 2.05 |
| **الإجمالي** | | | **7.1/10** |

**تفصيل الدرجات الفرعية:**

| المحور الهندسي | الدرجة | | المحور UX/UI | الدرجة | | المحور الأمني | الدرجة |
|---|---:|---|---|---:|---|---|---:|
| المعمارية | 8.5 | | الاتساق البصري | 6.0 | | الأسرار | 10 |
| القراءة والصيانة | 8.0 | | سهولة الاستخدام | 7.5 | | الحقن | 8 |
| DRY / Code Smells | **6.0** | | الاستجابة | 6.5 | | التحقق من المدخلات | 7 |
| معالجة الأخطاء | 6.5 | | العربية و RTL | 8.0 | | الصلاحيات | غ/م |
| الأداء | **5.5** | | الوصولية | **4.0** | | البيانات الحساسة | 9 |
| الاختبارات | 8.0 | | التغذية الراجعة | 6.5 | | الاعتماديات | 10 |
| التوثيق | 8.5 | | | | | CORS / Headers | **5** |
| التسمية والنمط | 8.0 | | | | | | |

**القراءة الصريحة للنتيجة:** المُصرِّف (engine/functions/helpers) يستحق ~8.5 وحده — عمل ناضج ومختبَر. الواجهة تسحب الدرجة للأسفل لأنها الجزء الوحيد بلا اختبارات، وفيها ثلاثة أعطال بصرية مؤكدة تظهر لكل مستخدم. **الأمر الجيد: كل عطل من الثلاثة إصلاحه من 3 إلى 10 أسطر.** الفجوة هنا ليست في الجودة الهندسية بل في **غياب حلقة تحقق للواجهة** — نفس الفريق الذي كتب 123 اختباراً للمُصرِّف لم يكتب اختباراً واحداً للطبقة التي يراها المستخدم.

---

## 6. خطة الإصلاح المرتبة بالأولوية

| # | الأولوية | الناحية | المشكلة | الحل المقترح | التأثير المتوقع |
|---|---|---|---|---|---|
| 1 | حرج 🔴 | UX | المحرر يعرض نصاً مزدوجاً مشوّهاً؛ التلوين التركيبي لا يظهر إطلاقاً — `<textarea>` يفرض `text-align:start` من UA stylesheet ولا يرث `right` من `.editor-wrap` (`index.html:112,124`) | `.editor-wrap{text-align:left}` + `.editor-input{color:transparent;caret-color:var(--primary);text-align:left}` + `.editor-highlight{color:var(--text)}` | **3 أسطر** — أول ما يراه المستخدم يصبح مقروءاً، وميزة معلنة في README تعمل فعلاً |
| 2 | حرج 🔴 | UX / هندسي | الكود الناتج يعرض `&#39;` بدل `'` — regex الأرقام (`ui.js:92`) يشطر الكيان `&#39;`. انحدار أدخله commit `f5a6f61` | لوكهيد `(?<!&#)` عاجلاً؛ وجذرياً: بناء العرض بـ`textContent` بدل جراحة regex على HTML | الناتج المنسوخ يصبح صحيحاً بصرياً + يغلق نمطاً هشّاً أمنياً |
| 3 | حرج 🔴 | هندسي / أمن | تكرار تقييم الوسائط → تضخم أسّي 3ⁿ: `ISBLANK` المتداخلة 11 مرة = **3.9 MB** ثم تجميد التبويب عند العرض (`functions.js:334,291,297,303,85,179,236,248`) | أداة `once(expr, body)` تُلزم التقييم مرة واحدة + حدّ على `expr.length` قبل العرض + اختبار عام "لا وسيط يتكرر" | يغلق DoS من جانب العميل + يزيل تكرار الحسابات + يقلّص حجم الناتج |
| 4 | حرج 🔴 | UX | الـToast غير مرئي في الوضع الليلي — أبيض على `#e8eaf0`، تباين **1.20:1** (`index.html:287-289`) | `background: var(--surface); color: var(--text); border: 1px solid var(--border-strong)` | التأكيد الوحيد لأهم إجراء (النسخ) يصبح مرئياً |
| 5 | حرج 🔴 | UX / وصولية | 18 بطاقة مثال غير قابلة للوصول بلوحة المفاتيح — `<div>` بلا `tabindex`/`role`/`keydown` (`ui.js:229-247`) | استبدال `<div>` بـ`<button type="button">` — يحل التنقل والدلالة والتفعيل معاً | ميزة الأمثلة تصبح متاحة لمستخدمي لوحة المفاتيح وقارئات الشاشة |
| 6 | مهم 🟠 | هندسي | مسافة أو سطر قبل `=` يكسر التحويل: `" =A1"` → `رمز غير متوقع` (`engine.js:22`, `ui.js:56`) | `input.trim()` مع تعديل `offset` للحفاظ على مواضع الأخطاء | يزيل إحباطاً شائعاً عند اللصق المباشر من Excel |
| 7 | مهم 🟠 | هندسي | فجوة اختبار كاملة: `ui.js` (305 سطراً) تغطيته **0%** — وهي بنيوية لأنه سكربت غير قابل للاستيراد | `initUI(root)` قابلة للتصدير + اختبارات jsdom للتظليل والنسخ والتصفية والثيم | يمنع تكرار العيوب 1–2–4 — الثلاثة كانت ستُلتقط باختبار DOM واحد |
| 8 | مهم 🟠 | هندسي | انحرافات Excel غير موثقة: `MOD(-3,2)`→`-1`، `ROUND(-2.5,0)`→`-2`، `1/0`→`Infinity`، `SQRT(-1)`→`NaN`، `SUM` يعدّ المنطقية | إصلاح `MOD` (`((a%b)+b)%b`) و`ROUND` (بعيداً عن الصفر) — رخيصان وشائعان؛ وتوثيق البقية في `README.md:272` | نتائج خاطئة صامتة → نتائج صحيحة أو انحراف معلن |
| 9 | مهم 🟠 | UX / وصولية | 5 إخفاقات تباين WCAG: أرقام الأسطر 1.93، ألوان `.tk-*` الليلية 1.40–3.34، Footer 2.83، `--text-mute` ليلي 3.10 | رفع `--text-mute` إلى `#6b7688`، أرقام الأسطر إلى `#7c8ba1`، وإضافة كتلة `[data-theme="dark"] .tk-*` | الأداة تصبح قابلة للاستخدام في الوضع الليلي وللمستخدمين ذوي ضعف البصر |
| 10 | مهم 🟠 | وصولية | `outline:none` يلغي مؤشر التركيز من المحرر (`index.html:125`) والبديل شفافية 2% | `.editor-input:focus-visible{outline:2px solid var(--accent);outline-offset:-2px}` | امتثال WCAG 2.4.7 |
| 11 | مهم 🟠 | UX | شبكة الأمثلة عمودان على 390px بينما التعليق نفسه يقول "عمود واحد" (`index.html:360-361`) | `grid-template-columns: 1fr` كما ينص التعليق | الأمثلة تصبح مقروءة فعلاً على الجوال |
| 12 | مهم 🟠 | أمن | لا حدود موارد: عمق تداخل، حجم ناتج، إجمالي خلايا، طول مُدخل | 4 حراس: عمق ≤64، خلايا ≤2000، طول مُدخل ≤10000، `expr.length` قبل العرض — مع رسائل عربية موضعية | يغلق DoS من جانب العميل ويستبدل `Maximum call stack size exceeded` برسالة مفهومة |
| 13 | مهم 🟠 | أمن | لا CSP، ولا `permissions` في CI workflow | `permissions: contents: read` في الـworkflow (سطر واحد)؛ وCSP بوسم meta بعد جعل `new Function` اختيارية | تحصين استباقي رخيص جداً |
| 14 | تحسين 🟡 | هندسي | تكرار سلسلة الترشيح الرقمي في 4 مولِّدات (`functions.js:65,71,77,200`) | helper `_nums(arr)` واحد | كود مولّد أصغر + نقطة تعديل واحدة |
| 15 | تحسين 🟡 | هندسي | Tokenizer O(n²) — `src.slice(pos).match()` لكل نمط (`engine.js:46`) | regex بعلم `y` (sticky) + `re.lastIndex = pos` | تعديل ميكانيكي على 13 سطراً يلغي التخصيص المتكرر |
| 16 | تحسين 🟡 | UX / أداء | إعادة تظليل كاملة عند كل ضغطة مفتاح بلا debounce (`ui.js:194`) | `requestAnimationFrame` أو debounce 60–100ms | كتابة أنعم على الصيغ الطويلة |
| 17 | تحسين 🟡 | هندسي | لا ESLint ولا Prettier ولا `.editorconfig` — الاتساق بشري بالكامل | `package.json` بـPrettier كـdevDependency + خطوة CI | يحمي الاتساق مع أول مساهم خارجي، بلا كسر مبدأ "صفر تبعيات تشغيل" |
| 18 | تحسين 🟡 | UX / RTL | خصائص فيزيائية بدل منطقية (`index.html:191,90,150`) + لا خط عربي في السلسلة | `border-inline-start` وأخواتها + `'Noto Sans Arabic'` في `font-family` | تصيير عربي أفضل + جاهزية لنسخة إنجليزية |
| 19 | تحسين 🟡 | UX | لا تحويل تلقائي، ولا بحث في الجدول المرجعي (44 صفاً)، ولا تأكيد على المسح | تحويل تلقائي بـdebounce 300ms + حقل بحث فوق الجدول + تراجع على المسح | يقلّل الاحتكاك في المسار الأساسي |
| 20 | تحسين 🟡 | هندسي | `SUBSTITUTE` مولِّد بسطر 231 حرفاً (`functions.js:191`)، وقوس بمسافة خاطئة (`:192`) | نقل المنطق إلى `_substitute` helper | يجعل أعقد مولِّد قابلاً للقراءة والتنقيح |
| 21 | تحسين 🟡 | هندسي | `_countifs` يفترض تساوي أطوال النطاقات (`helpers.js:147`) | فحص التساوي وإرجاع `#VALUE!` كما Excel | يزيل نتيجة خاطئة صامتة |
| 22 | تحسين 🟡 | وصولية | لا `<main>` ولا `<noscript>` ولا `role` على الـToast (بينما `tests.html:108` فيه `<main>`) | 3 أسطر HTML | بنية دلالية سليمة + تفسير عند تعطيل JS |

---

## 7. حدود المراجعة

- **ما تم التحقق منه بالتشغيل:** حزمة الاختبارات (123/123 ✓)، السلوك الدلالي لـ7 حالات حدّية بتنفيذ الكود المولّد فعلياً، قياس التضخم الأسّي حتى عمق 11، تنفيذ الواجهة في Chromium على 3 مقاسات مع لقطات شاشة، قراءة `getComputedStyle` لتشخيص عطل المحرر، وحساب نسب التباين رقمياً بمعادلة WCAG.
- **ما لم يُختبَر:** Safari و Firefox (الاختبار على Chromium فقط — عطل `text-align: start` على `<textarea>` سلوك UA وقد يختلف)، قارئات شاشة حقيقية (NVDA/VoiceOver — الملاحظات مبنية على غياب السمات لا على تجربة سمعية)، وأداء الأجهزة الضعيفة فعلياً.
- **ما لم يُراجَع لعدم وجوده:** لا backend، لا قاعدة بيانات، لا مصادقة، لا تبعيات — فالمحاور المقابلة في القسم 4 قُيِّمت بغيابها الصحيح لا بجودة تنفيذها.
