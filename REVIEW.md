# 🔍 المراجعة الهندسية الشاملة — excel-to-js (V3)

> مراجعة معمارية وأمنية وجودة كود للمشروع بالكامل، بتاريخ 2026-07-20.
> **منهجية:** قراءة كاملة للمستودع (7 ملفات، ~2800 سطر)، ثم **تحقق تجريبي** من كل ملاحظة مشتبه بها عبر تشغيل المحرك فعلياً في Node — لم تُدرج أي ملاحظة "خطأ" دون دليل تنفيذي. حزمة الاختبارات المئة شُغّلت كاملة وتأكد نجاحها 100/100.

---

## 1) الملخص العام

المشروع **أداة صغيرة مبنية بعناية واضحة**: مُحوّل صيغ Excel إلى JavaScript يعمل كموقع ثابت بدون أي تبعيات أو أدوات بناء. البنية الداخلية بنية *compiler* كلاسيكية نظيفة (Tokenizer → Parser → Generator) مع نظام helpers ذكي يولَّد عند الحاجة فقط ويُرتَّب topologically.

**التقييم الإجمالي: جيد جداً لحجمه ومرحلته** — لا توجد ثغرات أمنية ولا مشاكل أداء حقيقية، والاختبارات حقيقية وليست شكلية. المشاكل المكتشفة تتركز في **دقة محاكاة دلالات Excel** (وهي جوهر وعد الأداة): حالات تُنتج نتائج مختلفة عن Excel بصمت، وحالة تُنتج كوداً غير صالح نحوياً مع رسالة "نجح التحويل".

### نقاط القوة (نُفّذت باحترافية)

| النقطة | التفصيل |
|---|---|
| **معمارية المُحوّل** | فصل صريح بين tokenizer/parser/generator، وRecursive Descent صحيح. الأصعب: أولوية `^` (يسارية الترابط كما في Excel: `2^3^2=64`) وارتباط السالب الأحادي (`-2^2=4` كما في Excel) — **كلاهما صحيح**، وكثير من المحوّلات تخطئ فيهما. |
| **نظام الـ Helpers** | توليد عند الحاجة فقط + تبعيات مُعلنة + ترتيب topological. تصميم أنيق قابل للتوسع. |
| **رسائل الأخطاء الموضعية** | كل خطأ يحمل `start/end` ويظهر في الواجهة بتظليل متعرّج على الموضع الدقيق. تجربة مطوّر ممتازة. |
| **فلسفة الاختبار** | فئة Runtime تنفّذ الكود المولّد فعلياً بـ `new Function` — اختبار end-to-end حقيقي، وليس فقط مقارنة نصية. 100/100 ✓ (تحققت بالتشغيل). |
| **الأمان** | `escapeHtml` مطبّقة باتساق على كل مدخلات المستخدم ورسائل الأخطاء. الكود المولّد آمن من الحقن: النصوص عبر `JSON.stringify` وأسماء الخلايا مقيدة بـ regex صارم. لا تبعيات = لا مخاطر supply chain. |
| **تفاصيل التواريخ** | `EDATE` يعالج نهاية الشهر (31 يناير + شهر = 28 فبراير) بشكل صحيح، و`DATE` يحاكي ترحيل Excel للشهور. |
| **التوثيق** | README ممتاز: معمارية، قرارات تصميم، خارطة طريق، وقيود موثقة. |
| **الواجهة** | RTL كامل، وضع ليلي مع حفظ التفضيل واحترام تفضيل النظام، تحسينات جوال مدروسة، تظليل تركيبي حي. |

---

## 2) الملاحظات التفصيلية

> **لا توجد ملاحظات بدرجة Critical** — لا ثغرات أمنية ولا فقدان بيانات ولا انهيار. أعلى درجة مكتشفة: High.

---

### H1 — ‏`VLOOKUP`/`HLOOKUP`: تجاهل وضع المطابقة التقريبية + قلب الافتراضي

- **الوصف:** الـ helper ‏`_vlookup` يتجاهل باراميتر `exactMatch` كلياً ويبحث دائماً بمطابقة تامة (`row[0] === lookupValue`). إضافة لذلك، عند حذف الوسيط الرابع يمرر الـ generator ‏`true` (مطابقة تامة)، بينما الافتراضي في Excel عند الحذف هو **TRUE = مطابقة تقريبية** (أكبر قيمة ≤ في عمود مرتّب).
- **الدليل (تنفيذ فعلي):** `VLOOKUP(2.5, {1→10; 2→20; 3→30}, 2)` بدون وسيط رابع → الأداة تُرجع `#N/A`، بينما Excel يُرجع `20`.
- **لماذا مشكلة:** الأداة تَعِد بتحويل مكافئ لصيغ Excel؛ هنا تُنتج كوداً يعمل بلا خطأ لكنه يعطي **نتيجة مختلفة بصمت** — أخطر أنواع الأخطاء في مُحوّل.
- **الخطورة:** **High**
- **التأثير:** أي مستخدم ينقل جداول بحث تقريبية (شرائح ضريبية، درجات، عمولات — الاستخدام الأشهر للمطابقة التقريبية) سيحصل على `#N/A` أو نتائج خاطئة.
- **أفضل الممارسات:** محاكاة الدلالات المرجعية حرفياً، أو رفض الحالة غير المدعومة برسالة صريحة — لا التنفيذ الصامت الخاطئ.
- **الحلول:**
  1. **تنفيذ المطابقة التقريبية** في `_vlookup`/`_hlookup` (مسح خطي يحتفظ بآخر قيمة ≤) وتصحيح الافتراضي عند الحذف. تكلفة: ~15 سطراً + اختبارات.
  2. **رفض الحذف والقيمة TRUE** برسالة "المطابقة التقريبية غير مدعومة، مرر FALSE". أبسط لكنه يُضعف التغطية.
  3. إبقاء الوضع الحالي مع توثيق القيد. الأسوأ — يُبقي النتائج الخاطئة الصامتة.
- **التوصية:** الحل 1. التنفيذ رخيص والدلالة معروفة، وهو يزيل أخطر انحراف دلالي في الأداة. (وحتى إتمامه، الحل 2 كسدّ مؤقت أفضل من الصمت.)

---

### H2 — غياب التحقق من عدد الوسائط يُنتج كوداً غير صالح مع رسالة نجاح

- **الوصف:** التحقق من الـ arity موزّع يدوياً وغير متسق: `IF`, `POWER`, `MID`… تتحقق، بينما `AND`, `OR`, `NOT`, `SQRT`, `ABS`, `IFERROR` وغيرها لا تتحقق.
- **الدليل (تنفيذ فعلي):**
  - `=AND()` → يولّد `return ();` — **خطأ نحوي في JS**، والواجهة تعرض "✓ نجح التحويل".
  - `=NOT()` → يولّد `(!undefined)` → يُرجع `true` دائماً (Excel: خطأ).
  - `=IFERROR(A1)` → يولّد `undefined` كقيمة بديلة (Excel: يرفض الصيغة).
- **لماذا مشكلة:** الأداة تُبلغ نجاحاً عن مخرجات إما لا تُصرَّف أصلاً أو تحمل دلالة خاطئة. الثقة في زر "نجح التحويل" هي المنتج نفسه.
- **الخطورة:** **High** (كسر للعقد الأساسي للأداة، وإن كانت المدخلات المسببة نادرة)
- **التأثير:** المستخدم ينسخ كوداً مكسوراً إلى مشروعه ويكتشف الخطأ متأخراً بعيداً عن سياق الأداة.
- **أفضل الممارسات:** التحقق التصريحي (declarative) من العقود في تعريف كل دالة بدل شروط يدوية متناثرة.
- **الحلول:**
  1. **إضافة `minArgs`/`maxArgs` لتعريف كل دالة** في `FUNCTIONS` مع فحص مركزي واحد في `generate()` قبل استدعاء `generator`. يوحّد الرسائل، يحذف ~10 فحوصات مكررة، ويغطي الـ 44 دالة دفعة واحدة.
  2. إضافة فحوصات يدوية للدوال الناقصة فقط. يسد الثغرة لكنه يفاقم التكرار الموجود أصلاً.
  3. فحص لاحق للمخرجات (parse الكود المولّد بـ `new Function` قبل العرض). شبكة أمان جيدة لكنها تكشف الخطأ ولا تشرحه للمستخدم بموضع دقيق.
- **التوصية:** الحل 1 (وهو أيضاً *تبسيط* للكود لأنه يحذف التكرار)، مع الحل 3 كطبقة دفاع ثانية رخيصة (سطران) تلتقط أي فئة أخطاء مستقبلية من نفس النوع.

---

### H3 — ‏`DATEDIF` بوحدتي `D`/`YD`: انزياح يوم كامل عبر حدود التوقيت الصيفي

- **الوصف:** حساب الأيام يقسم فرق الميلي ثانية بين تاريخين محليين على 86,400,000. عند عبور الفترة حدّ توقيت صيفي يصبح أحد الأيام 23 ساعة، فيعطي `Math.floor` ناتجاً أقل بيوم.
- **الدليل (تنفيذ فعلي بمنطقة `America/New_York`):** `DATEDIF(DATE(2026,1,1), DATE(2026,7,1), "D")` → **180**، والصحيح 181 (وExcel يُرجع 181).
- **لماذا مشكلة:** نتيجة خاطئة بصمت تعتمد على المنطقة الزمنية لجهاز *مستخدم الكود المولّد* — أسوأ أنواع الأخطاء قابليةً لإعادة الإنتاج.
- **الخطورة:** **High** (خطأ حسابي صامت في دالة تواريخ أساسية؛ لا يظهر في مناطق بلا DST مثل السعودية، ويظهر في أوروبا/أمريكا)
- **التأثير:** حسابات أعمار، مدد عقود، فوائد… تخطئ بيوم واحد بشكل متقطع يصعب تشخيصه.
- **أفضل الممارسات:** حساب فروق الأيام التقويمية عبر `Date.UTC` من مكونات التاريخ، وليس عبر طرح timestamps محلية.
- **الحلول:**
  1. **التطبيع إلى UTC:** `(Date.UTC(eY,eM,eD) - Date.UTC(sY,sM,sD)) / 86400000` في فرعي `D` و`YD`. سطران، صحيح دائماً.
  2. `Math.round` بدل `Math.floor`. يخفي العَرَض في حالة DST لكنه يكسر الحالات التي فيها فرق وقت فعلي داخل اليوم (تواريخ تحمل ساعات).
- **التوصية:** الحل 1 — هو الحل القياسي الوحيد الصحيح. مع إضافة اختبار يثبّت التاريخين حول عبور DST.

---

### M1 — انحرافات دلالية عن Excel غير موثقة (تُنتج نتائج مختلفة بصمت)

- **الوصف والدليل (كلها منفذة فعلياً):**
  | الحالة | ناتج الأداة | ناتج Excel |
  |---|---|---|
  | `="abc"="ABC"` | `false` (`===` حساس للحالة) | `TRUE` (المقارنة النصية غير حساسة للحالة) |
  | `=10%3` | `1` (يعامل `%` كباقي قسمة ثنائي) | لا يوجد عامل `%` ثنائي في Excel؛ `%` لاحقة نسبة مئوية (`50%` = 0.5) |
  | `=MIN(A1:A2)` بنصوص فقط | `Infinity` | `0` |
  | `=AVERAGE(A1:A2)` بنصوص فقط | `0` | `#DIV/0!` |
  | `COUNTIF` مع `*`/`?` | مطابقة حرفية | wildcards |
  | `SUM` مع نص رقمي في نطاق | يجمعه (`Number(b)||0`) | يتجاهله |
- **لماذا مشكلة:** كل واحدة على حدة صغيرة، لكن مجموعها يقوّض وعد "تحويل مكافئ". الأخطر أنها صامتة.
- **الخطورة:** **Medium** (High لحساسية الحالة في `=` لأنها شائعة جداً في `IF`/`VLOOKUP` على نصوص)
- **التأثير:** نتائج مختلفة عن الجدول الأصلي في صيغ شائعة، بلا أي تحذير.
- **الحلول:**
  1. **محاكاة دلالات Excel** حيث يكون رخيصاً: مقارنة نصية غير حساسة للحالة عبر helper ‏`_eq` (يُفعَّل عند مقارنة `=`/`<>`)، `%` لاحقة بدل ثنائية (أو رفضها برسالة)، `MIN`/`MAX` فارغة → 0، `AVERAGE` فارغة → `#DIV/0!`.
  2. **توثيق جدول انحرافات** في README + تحذير في الواجهة عند استخدام الأنماط المتأثرة.
  3. **وضعان:** "JS طبيعي" (الحالي) و"توافق Excel صارم". الأدق لكنه يضاعف مصفوفة الاختبار.
- **التوصية:** مزيج 1+2: أصلِح الرخيص عالي الأثر (المقارنة النصية، `%`، الحدّيات الفارغة)، ووثّق الباقي في جدول انحرافات صريح. الحل 3 مؤجل حتى يطلبه مستخدمون فعليون.
- **درجة الثقة:** سلوك الأداة مؤكد بالتنفيذ؛ سلوك Excel المقابل من معرفة راسخة بدلالاته — يُستحسن التثبيت النهائي بلقطة من Excel حقيقي عند كتابة اختبارات التوافق.

---

### M2 — لا يوجد ملف LICENSE رغم إعلان MIT

- **الوصف:** README يحمل شارة MIT ورابطاً إلى `LICENSE`، والملف غير موجود في المستودع (تحققت: `NO LICENSE FILE`).
- **لماذا مشكلة:** بدون نص رخصة، القانون الافتراضي هو "جميع الحقوق محفوظة" — إعلان MIT في README غير كافٍ قانونياً، والرابط في الشارة مكسور.
- **الخطورة:** **Medium** (قانوني، لا تقني)
- **الحل والتوصية:** إضافة ملف `LICENSE` بنص MIT القياسي واسم صاحب الحقوق. دقيقة عمل واحدة، لا بدائل تستحق المقارنة.

---

### M3 — لا يوجد CI: الاختبارات لا تُشغَّل إلا يدوياً في متصفح

- **الوصف:** لا مجلد `.github/workflows`. الاختبارات ممتازة لكنها تتطلب فتح `tests.html` يدوياً؛ لا شيء يمنع دمج commit يكسرها.
- **لماذا مشكلة:** قيمة الاختبارات تتحقق فقط إذا شُغّلت عند كل تغيير. شارة "100/100" في README ستتقادم بصمت.
- **الخطورة:** **Medium** (مخاطرة عملية تتضاعف مع خارطة الطريق النشطة: 7 مراحل قادمة)
- **التأثير:** ارتدادات (regressions) غير مكتشفة، خصوصاً أن كل دالة جديدة تلمس الـ generator المشترك.
- **الحلول:**
  1. **GitHub Actions + Node مباشرة:** المحرك pure JS بلا DOM؛ **أثبتّ عملياً** أن الحزمة كاملة تعمل في Node عبر shim صغير (~40 سطراً يوفر `test/assert*/runFormula`). بلا أي تبعية.
  2. Playwright يفتح `tests.html` فعلياً. يختبر الحقيقة الكاملة لكنه يجلب تبعية ثقيلة تناقض فلسفة المشروع.
- **التوصية:** الحل 1: استخراج دوال الـ assertions إلى `tests/framework.js` يُحمَّل من `tests.html` **ومن** سكربت Node، ثم workflow من ~15 سطراً. يبقى المشروع بلا تبعيات ويصبح كل push مُختبَراً.

---

### M4 — الحالة العامة (globals) والاعتماد على ترتيب `<script>`

- **الوصف:** الملفات الثلاثة تُعرّف globals (`HELPERS`, `FUNCTIONS`, `tokenize`, …) ويعتمد كل شيء على ترتيب التحميل (موثّق بتعليق فقط).
- **لماذا مشكلة:** تلوث نطاق عام، وخطأ ترتيب يظهر كـ `ReferenceError` غامض وقت التشغيل، ولا يمكن استيراد المحرك كوحدة في مشاريع أخرى (رغم أن README يقترح استخدامه كـ submodule).
- **الخطورة:** **Low–Medium**
- **قيد مهم:** ES Modules **لا تعمل عبر `file://`**، والمشروع يَعِد صراحة بـ "افتح index.html مباشرة" — لذا التحويل الكامل لـ ESM يكسر ميزة معلنة.
- **الحلول:**
  1. **نمط UMD خفيف بلا بناء:** كل ملف يلتف بـ IIFE ويصدّر عبر `window.ExcelToJS = {...}` وفي Node عبر `module.exports`. يحافظ على `file://` ويجعل الاستيراد في Node (للـ CI) نظيفاً بدل `eval`.
  2. ESM + سيرفر تطوير. أنظف معيارياً لكنه يكسر وعد `file://`.
  3. إبقاء الوضع مع فحص وجود صريح (`if (typeof HELPERS === 'undefined') throw ...`) في رأس `engine.js`.
- **التوصية:** الحل 1، وإن ضاق الوقت فالحل 3 كحد أدنى (سطر واحد يحوّل خطأ غامضاً إلى رسالة واضحة).

---

### ملاحظات Low (وصف مركّز + توصية مباشرة)

| # | الملاحظة | التفصيل والتوصية |
|---|---|---|
| L1 | **العدد "44 دالة" مكتوب يدوياً في 4 مواضع** (شارتان + عنوان قسم + footer + README) | سينكسر مع المرحلة 3.2 (13 دالة جديدة). يُحسب من `Object.keys(FUNCTIONS).length` في الواجهة، ويبقى README يدوياً بفحص CI بسيط. |
| L2 | **تكرار CSS:** `.convert-bar` معرّفة مرتين في index.html (سطر 180 و314) بقيم متداخلة | حذف التعريف الأول. |
| L3 | **Tokenizer:** regex النصوص يدعم escape بـ backslash (`\\.`) وهذا ليس من دلالات Excel (فقط `""`)؛ الترميز العلمي `1E5` يفشل برسالة مضللة ("رمز إضافي: E5" — تحققت)؛ النطاق بمسافات `A1 : A3` مرفوض وExcel يقبله | إزالة فرع `\\.`، ودعم `\d+(\.\d+)?([Ee][+-]?\d+)?`، والسماح بمسافات حول `:` — كلها تعديلات regex صغيرة مع اختبار لكل حالة. |
| L4 | **`IFERROR` يكرر تعبير الـ fallback مرتين** في الكود المولّد (في `try` وفي `catch`) — قسته: تعمّق 3 مستويات = تضخم محدود (556→710 حرفاً) لكنه أسّي نظرياً مع fallback ضخم | توليد `const _f = () => (fallback)` مرة واحدة واستدعاؤها في الموضعين. |
| L5 | **`highlightJS` بالـ regex المتسلسل** قد يفتح spans متداخلة (كلمة `function` داخل تعليق ملوّن مسبقاً) — أثر بصري فقط | مقبول حالياً؛ عند التوسع استخدم tokenizer صغيراً للعرض بدل السلاسل. |
| L6 | **Accessibility:** الـ textarea بلا `aria-label` (placeholder فقط)، زر الثيم emoji بلا `aria-label`، أزرار التصفية بلا `aria-pressed`، الحالة `#status` بلا `role="status"`/`aria-live` | 4 خصائص HTML تُضاف في دقائق وترفع الوصولية فعلياً. |
| L7 | **`localStorage.setItem` قد يرمي استثناء** في بعض أوضاع الخصوصية → يعطّل بقية الـ handler | لفّها بـ try/catch. |
| L8 | **`String.prototype.substr` مهجورة** في `MID` | استبدالها بـ `substring(start-1, start-1+len)`. |
| L9 | **وميض ثيم (FOUC):** الثيم يُطبَّق في نهاية `<body>`؛ مستخدم الوضع الليلي يرى ومضة فاتحة | نقل سطري قراءة التفضيل والتطبيق إلى `<script>` مضمّن في `<head>`. |
| L10 | **`escapeHtml` لا تهرّب علامات الاقتباس** — آمنة حالياً لأن كل الاستخدامات في محتوى عناصر لا في attributes، لكنها قابلة للكسر عند أول استخدام مستقبلي داخل attribute | إضافة `&quot;`/`&#39;` كتحصين استباقي. |
| L11 | **حد الـ 1000 خلية يولّد `calculate(a1, …, a1000)`** — صالح نحوياً لكنه غير قابل للاستخدام عملياً | ضمن مراحل الخارطة: وضع إخراج بديل يستقبل object واحداً (`calculate(cells)`) عند تجاوز عتبة (مثلاً 20 خلية). |
| L12 | **أداء الـ tokenizer** ‏`src.slice(pos).match()` يعيد إنشاء سلسلة عند كل token — O(n²) نظرياً، **بلا أثر حقيقي** لأحجام الصيغ الواقعية | لا إجراء الآن؛ عند الحاجة regex بعلم `y` (sticky). مذكورة للأمانة لا كمشكلة فعلية. |
| L13 | **تاريخ git:** commits باسم "Add files via upload" | اعتماد رسائل وصفية من الآن فصاعداً (لا حاجة لإعادة كتابة التاريخ). |

---

## 3) أبرز المخاطر التقنية

1. **الانحراف الدلالي الصامت عن Excel** (H1, H3, M1) — المستخدم يثق بالمخرجات دون وسيلة للتحقق داخل الأداة.
2. **رسالة نجاح على مخرجات مكسورة** (H2) — كسر لعقد الثقة الأساسي.
3. **غياب CI مع خارطة طريق نشطة** (M3) — كل مرحلة قادمة تلمس generator مشتركاً؛ احتمال الارتداد يتراكم.
4. **الغموض القانوني** (M2) — يمنع تبنّي المشروع في بيئات تشترط رخصة صريحة.

## 4) الأخطاء المؤكدة مرتبة بالأولوية

| الأولوية | الخطأ | الخطورة | الدليل |
|---|---|---|---|
| 1 | `VLOOKUP`/`HLOOKUP`: تجاهل المطابقة التقريبية + قلب الافتراضي | High | `VLOOKUP(2.5,…)` → `#N/A` بدل `20` |
| 2 | `=AND()` يولّد `return ();` (خطأ نحوي) مع رسالة نجاح؛ `NOT()`/`IFERROR(x)` بدلالة خاطئة | High | منفذ فعلياً |
| 3 | `DATEDIF "D"/"YD"` تنقص يوماً عبر حدود DST | High | 180 بدل 181 في `America/New_York` |
| 4 | مقارنة `=` النصية حساسة للحالة بعكس Excel | Medium | `"abc"="ABC"` → `false` |
| 5 | `%` يُفسَّر باقي قسمة ثنائياً وExcel لا يملك هذا العامل | Medium | `10%3` → `1` |
| 6 | `MIN`/`MAX` بلا أرقام → `Infinity`؛ `AVERAGE` → `0` بدل `#DIV/0!` | Medium | منفذ فعلياً |
| 7 | شارة MIT بلا ملف LICENSE | Medium | غياب الملف |
| 8 | `1E5` يفشل برسالة مضللة؛ `A1 : A3` مرفوض | Low | منفذ فعلياً |

## 5) فرص التحسين حسب المحور

- **الأداء:** لا اختناقات فعلية (الاختبارات المئة في ~10ms، والتظليل الحي على صيغ قصيرة). الفرص المتاحة (sticky regex، عدم إعادة بناء DOM للجدول المرجعي) كلها تحسينات نظرية لا تستحق الأولوية الآن — **وهذه شهادة إيجابية لا سلبية**.
- **الأمان:** الوضع جيد. تحصينات رخيصة: تهريب الاقتباسات في `escapeHtml` (L10)، وإضافة `<meta http-equiv="Content-Security-Policy" content="default-src 'self'; style-src 'unsafe-inline'; script-src 'self' 'unsafe-inline'">` كطبقة دفاع لموقع GitHub Pages، وتوثيق أن الكود المولّد مصمم ليُنسخ ويُراجع لا ليُنفَّذ تلقائياً.
- **المعمارية:** الفصل الثلاثي ممتاز؛ الفرص: عقد `minArgs/maxArgs` التصريحي (H2)، نمط UMD للتصدير (M4)، ونقل سكربت الواجهة (~300 سطر داخل index.html) إلى `assets/ui.js` ليكتمل الفصل وتصبح index.html هيكلاً فقط.
- **تبسيط الكود وتقليل التعقيد:** الفحص المركزي للـ arity يحذف ~10 شروط مكررة؛ توحيد نمط "تاريخ أو تحويل" المكرر في `YEAR/MONTH/DAY` بـ helper ‏`_toDate` واحد؛ حذف CSS المكرر؛ حساب العدد 44 ديناميكياً.
- **تجربة المطوّر (DX):** CI بـ Node (مثبت الجدوى)، `tests/framework.js` مشترك، `CONTRIBUTING.md` قصير يشرح كيفية إضافة دالة (خطوتان: تعريف في `FUNCTIONS` + اختبار Runtime — البنية الحالية تجعلها سهلة فعلاً وهذا يستحق الإعلان)، وJSDoc على `convertFormula` و`generate`.
- **الديون التقنية:** المشروع شاب وديونه قليلة؛ أثقلها M1 (الانحرافات غير الموثقة) لأنها تكبر مع كل دالة جديدة تُبنى فوق نفس الافتراضات. جدول انحرافات موثّق يجمّد هذا الدين.

## 6) اقتراحات إعادة الهيكلة

1. `FUNCTIONS` يصبح العقد الكامل: `{ cat, desc, jsEquiv, minArgs, maxArgs, matrixArgs?, usesHelpers?, generator }` — فحص مركزي + إمكانية توليد جدول التوثيق تلقائياً من نفس المصدر.
2. استخراج سكربت الواجهة إلى `assets/ui.js`.
3. `tests/framework.js` مشترك بين المتصفح وNode.
4. لفّ الملفات بنمط UMD خفيف (بلا أدوات بناء).

## 7) ميزات مقترحة ترفع الجودة

- **وضع "توافق Excel" مع تحذيرات حية:** عند استخدام نمط منحرف (`%`، مقارنة نصية، دالة بحدّيات فارغة محتملة) تظهر ملاحظة تحذيرية تحت الناتج — يحوّل جدول الانحرافات من توثيق سلبي إلى ميزة.
- **اختبار خصائص (property/fuzz testing):** مولّد صيغ عشوائية من الدوال المدعومة يتحقق أن كل ناتج (أ) يُصرَّف بلا خطأ نحوي (ب) لا يرمي استثناء غير متوقع — كان سيلتقط `AND()` تلقائياً.
- **وضع إخراج object-parameter** للنطاقات الكبيرة (L11).
- بقية خارطة الطريق الحالية (3.2–3.8) وجيهة كما هي؛ أنصح بتقديم 3.5 (رسائل "ربما تقصد؟") لأنها رخيصة وأثرها على UX كبير.

## 8) خطة التطوير التدريجية (الأثر ÷ التكلفة تنازلياً)

| المرحلة | المحتوى | التكلفة | الأثر |
|---|---|---|---|
| **0 — فوري** | ملف LICENSE + حذف CSS المكرر + العدد الديناميكي | ساعة | يغلق M2, L1, L2 |
| **1 — تصحيحات الدقة** | H1 (المطابقة التقريبية) + H3 (UTC في DATEDIF) + H2 (فحص arity مركزي) مع اختبار لكل إصلاح | يوم | يغلق كل الـ High |
| **2 — CI** | `tests/framework.js` + workflow يشغّل الحزمة في Node | نصف يوم | كل push مُختبَر؛ يؤمّن المراحل القادمة |
| **3 — الدلالات** | مقارنة نصية غير حساسة للحالة، معالجة `%`، الحدّيات الفارغة + جدول انحرافات موثّق | 1–2 يوم | يغلق M1 |
| **4 — الجودة الهيكلية** | UMD + استخراج ui.js + الوصولية (L6) + بقية Low | 1–2 يوم | يغلق M4 ومعظم Low |
| **5 — الميزات** | استئناف خارطة الطريق (3.2+) فوق أساس مُختبَر آلياً | حسب الخارطة | — |

> **المنطق:** إصلاح الدقة قبل CI قد يبدو معكوساً، لكن إصلاحات المرحلة 1 صغيرة ومختبرة يدوياً، بينما المرحلة 3 والميزات أوسع فيجب أن يسبقها CI. البديل (CI أولاً) مقبول أيضاً إن فُضّل — الفارق يوم واحد.

---

## 9) حدود المراجعة ودرجات الثقة

- كل ما وُصف بـ"خطأ" **مؤكد بالتنفيذ الفعلي** على محرك المشروع في Node (بيئة `America/New_York` لحالة DST).
- القيم المرجعية لسلوك Excel من معرفة راسخة بدلالاته؛ ثقة عالية جداً في حالات H1 وM1، ويُستحسن تثبيتها بلقطات من Excel حقيقي عند بناء اختبارات التوافق.
- لم تُراجع إعدادات GitHub Pages نفسها (خارج المستودع).
- لم يُنفَّذ أي تعديل على كود المشروع ضمن هذه المراجعة — هذا الملف إضافة توثيقية فقط.
